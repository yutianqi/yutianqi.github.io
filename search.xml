<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Raspberry Pi配置记录</title>
    <url>/2018/02/13/Raspberry-Pi/</url>
    <content><![CDATA[<p>本文为Raspberry Pi配置记录。</p>
<a id="more"></a>

<h3 id="1-系统烧录"><a href="#1-系统烧录" class="headerlink" title="1 系统烧录"></a>1 系统烧录</h3><h4 id="1-1-下载树莓派系统"><a href="#1-1-下载树莓派系统" class="headerlink" title="1.1 下载树莓派系统"></a>1.1 下载树莓派系统</h4><p><a href="https://www.raspberrypi.org/downloads/">RaspberryPi官网系统下载页面</a>       </p>
<h4 id="1-2-下载Win32DiskImager"><a href="#1-2-下载Win32DiskImager" class="headerlink" title="1.2 下载Win32DiskImager"></a>1.2 下载Win32DiskImager</h4><p><a href="http://sourceforge.net/projects/win32diskimager/files/Archive/win32diskimager-v0.9-binary.zip/download">下载镜像烧录工具Win32DiskImager</a>      </p>
<h4 id="1-3烧录镜像到SD卡"><a href="#1-3烧录镜像到SD卡" class="headerlink" title="1.3烧录镜像到SD卡"></a>1.3烧录镜像到SD卡</h4><ul>
<li>解压.zip镜像压缩文件</li>
<li>启动win32diskimager</li>
<li>选择.img镜像文件(Image)</li>
<li>选择写入设备(Device)</li>
<li>写入(Write)</li>
</ul>
<h3 id="2-开启ssh服务"><a href="#2-开启ssh服务" class="headerlink" title="2 开启ssh服务"></a>2 开启ssh服务</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo service sshd start </span><br></pre></td></tr></table></figure>

<h3 id="3-命令行下配置wifi信息"><a href="#3-命令行下配置wifi信息" class="headerlink" title="3 命令行下配置wifi信息"></a>3 命令行下配置wifi信息</h3><h3 id="4-软件源更改"><a href="#4-软件源更改" class="headerlink" title="4 软件源更改"></a>4 软件源更改</h3><p>由于树莓派系统默认官方软件源在国外，连接不稳定且速度慢，所以安装初次进入系统后，建议修改软件源。<br>以下以中国科学技术大学Raspbian软件源为例：<a href="http://mirrors.ustc.edu.cn/raspbian/raspbian/">http://mirrors.ustc.edu.cn/raspbian/raspbian/</a></p>
<h4 id="4-1-操作指令"><a href="#4-1-操作指令" class="headerlink" title="4.1 操作指令"></a>4.1 操作指令</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 该脚本适配最新的 2017-11-29-raspbian-stretch(基于Debian9)</span><br><span class="line">sudo -s</span><br><span class="line">echo -e &quot;deb http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;raspbian&#x2F;raspbian&#x2F; stretch main contrib non-free rpi \n deb-src http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;raspbian&#x2F;raspbian&#x2F; stretch main contrib non-free rpi&quot; &gt; &#x2F;etc&#x2F;apt&#x2F;sources.list</span><br><span class="line">echo -e &quot;deb http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;archive.raspberrypi.org&#x2F; stretch main ui&quot; &gt; &#x2F;etc&#x2F;apt&#x2F;sources.list.d&#x2F;raspi.list</span><br><span class="line">exit</span><br><span class="line">sudo apt update &amp;&amp; sudo apt -y upgrade</span><br><span class="line"></span><br><span class="line"># 该脚本适配 2017-06-21-raspbian-jessie(基于Debian8)</span><br><span class="line">sudo -s</span><br><span class="line">echo -e &quot;deb http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;raspbian&#x2F;raspbian&#x2F; jessie main non-free contrib \n deb-src http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;raspbian&#x2F;raspbian&#x2F; jessie main non-free contrib&quot; &gt; &#x2F;etc&#x2F;apt&#x2F;sources.list</span><br><span class="line">echo -e &quot;deb http:&#x2F;&#x2F;mirrors.ustc.edu.cn&#x2F;archive.raspberrypi.org&#x2F; jessie main&quot; &gt; &#x2F;etc&#x2F;apt&#x2F;sources.list.d&#x2F;raspi.list</span><br><span class="line">exit</span><br><span class="line">sudo apt-get update &amp;&amp; sudo apt-get -y upgrade</span><br><span class="line"></span><br><span class="line"># 非该版本请自行打开（&#x2F;etc&#x2F;apt&#x2F;sources.list 和 &#x2F;etc&#x2F;apt&#x2F;sources.list.d&#x2F;raspi.list）替换网址即可</span><br></pre></td></tr></table></figure>


<h4 id="4-2-其它软件源"><a href="#4-2-其它软件源" class="headerlink" title="4.2 其它软件源"></a>4.2 其它软件源</h4><table>
<thead>
<tr>
<th>Raspbian软件源</th>
<th>URL</th>
</tr>
</thead>
<tbody><tr>
<td>中山大学</td>
<td><a href="http://mirror.sysu.edu.cn/raspbian/raspbian/">http://mirror.sysu.edu.cn/raspbian/raspbian/</a></td>
</tr>
<tr>
<td>清华大学</td>
<td><a href="http://mirrors.tuna.tsinghua.edu.cn/raspbian/raspbian/">http://mirrors.tuna.tsinghua.edu.cn/raspbian/raspbian/</a></td>
</tr>
<tr>
<td>华中科技大学</td>
<td><a href="http://mirrors.hustunique.com/raspbian/raspbian/Arch">http://mirrors.hustunique.com/raspbian/raspbian/Arch</a></td>
</tr>
<tr>
<td>Linux ARM</td>
<td><a href="http://mirrors.hustunique.com/archlinuxarm/">http://mirrors.hustunique.com/archlinuxarm/</a></td>
</tr>
<tr>
<td>大连东软信息学院</td>
<td><a href="http://mirrors.neusoft.edu.cn/raspbian/raspbian/">http://mirrors.neusoft.edu.cn/raspbian/raspbian/</a></td>
</tr>
<tr>
<td>重庆大学</td>
<td><a href="http://mirrors.cqu.edu.cn/Raspbian/raspbian/">http://mirrors.cqu.edu.cn/Raspbian/raspbian/</a></td>
</tr>
<tr>
<td>阿里云</td>
<td><a href="http://mirrors.aliyun.com/raspbian/raspbian/">http://mirrors.aliyun.com/raspbian/raspbian/</a></td>
</tr>
</tbody></table>
<h3 id="5-中文支持及中文输入法"><a href="#5-中文支持及中文输入法" class="headerlink" title="5 中文支持及中文输入法"></a>5 中文支持及中文输入法</h3><h4 id="5-1-操作指令"><a href="#5-1-操作指令" class="headerlink" title="5.1 操作指令"></a>5.1 操作指令</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install -y ttf-wqy-zenhei</span><br><span class="line">sudo apt-get install -y scim-pinyin</span><br></pre></td></tr></table></figure>

<h3 id="6-安装vim"><a href="#6-安装vim" class="headerlink" title="6 安装vim"></a>6 安装vim</h3><h4 id="6-1-操作指令"><a href="#6-1-操作指令" class="headerlink" title="6.1 操作指令"></a>6.1 操作指令</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install -y vim</span><br></pre></td></tr></table></figure>
<h4 id="6-2-如出现如下错误，先卸载-vim-common-再安装vim即可"><a href="#6-2-如出现如下错误，先卸载-vim-common-再安装vim即可" class="headerlink" title="6.2 如出现如下错误，先卸载 vim-common 再安装vim即可"></a>6.2 如出现如下错误，先卸载 vim-common 再安装vim即可</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">The following packages have unmet dependencies: </span><br><span class="line">vim : Depends: vim-common (&#x3D; 2:7.4.488-7+deb8u3) but 2:8.0.0197-4 is to be installed </span><br><span class="line">E: Unable to correct problems, you have held broken packages.</span><br><span class="line"></span><br><span class="line">sudo apt-get remove vim-common</span><br><span class="line">sudo apt-get install -y vim</span><br></pre></td></tr></table></figure>


<h3 id="7-修改时区-时间"><a href="#7-修改时区-时间" class="headerlink" title="7 修改时区/时间"></a>7 修改时区/时间</h3><h4 id="7-1-操作指令"><a href="#7-1-操作指令" class="headerlink" title="7.1 操作指令"></a>7.1 操作指令</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo dpkg-reconfigure tzdata</span><br><span class="line">sudo apt-get install ntpdate</span><br></pre></td></tr></table></figure>


<h3 id="8-通过xrdp服务windows远程桌面连接树莓派"><a href="#8-通过xrdp服务windows远程桌面连接树莓派" class="headerlink" title="8 通过xrdp服务windows远程桌面连接树莓派"></a>8 通过xrdp服务windows远程桌面连接树莓派</h3><h4 id="8-1-操作指令"><a href="#8-1-操作指令" class="headerlink" title="8.1 操作指令"></a>8.1 操作指令</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 安装tightvncserver</span><br><span class="line">sudo apt-get install tightvncserver</span><br><span class="line"># 安装xrdp服务</span><br><span class="line">sudo apt-get install xrdp</span><br><span class="line"># 如已开启防火墙ufw，则需打开服务器上的远程桌面访问端口，并重启服务</span><br><span class="line">sudo ufw allow 3389</span><br><span class="line">sudo service ufw restart</span><br><span class="line"># 重启xrdp服务</span><br><span class="line">sudo service xrdp restart </span><br></pre></td></tr></table></figure>




<h3 id="9-U盘操作"><a href="#9-U盘操作" class="headerlink" title="9 U盘操作"></a>9 U盘操作</h3><h4 id="9-1-挂载U盘"><a href="#9-1-挂载U盘" class="headerlink" title="9.1 挂载U盘"></a>9.1 挂载U盘</h4><ul>
<li>插入U盘，执行fdisk -l查看U盘信息<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># &#x2F;dev&#x2F;mmc表示TF卡</span><br><span class="line"># &#x2F;dev&#x2F;sda&#x2F;表示第一个硬件(U盘)</span><br><span class="line"># 以下&#x2F;dev&#x2F;sda1即为U盘第一个(也是唯一一个)分区</span><br><span class="line"></span><br><span class="line">Disk &#x2F;dev&#x2F;sda: 7.5 GiB, 8022654976 bytes, 15669248 sectors</span><br><span class="line">Units: sectors of 1 * 512 &#x3D; 512 bytes</span><br><span class="line">Sector size (logical&#x2F;physical): 512 bytes &#x2F; 512 bytes</span><br><span class="line">I&#x2F;O size (minimum&#x2F;optimal): 512 bytes &#x2F; 512 bytes</span><br><span class="line">Disklabel type: dos</span><br><span class="line">Disk identifier: 0xcad4ebea</span><br><span class="line"></span><br><span class="line">Device     Boot Start      End  Sectors  Size Id Type</span><br><span class="line">&#x2F;dev&#x2F;sda1  *      256 15669247 15668992  7.5G  7 HPFS&#x2F;NTFS&#x2F;exFAT</span><br></pre></td></tr></table></figure></li>
<li>创建挂载路径<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir &#x2F;mnt&#x2F;usb_flash</span><br></pre></td></tr></table></figure></li>
<li>挂载设备<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mount &#x2F;dev&#x2F;sda1 &#x2F;mnt&#x2F;usb_flash&#x2F;                     # 普通挂载</span><br><span class="line">mount -o uid&#x3D;pi,gid&#x3D;pi &#x2F;dev&#x2F;sda1 &#x2F;mnt&#x2F;usb_flash&#x2F;    # 使用特定配置挂载</span><br></pre></td></tr></table></figure></li>
<li>如果U盘设备为NTFS格式，默认只具备读权限，如需写入权限，需要额外安装软件包<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">apt-get install ntfs-3g</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h4 id="9-2-卸载U盘"><a href="#9-2-卸载U盘" class="headerlink" title="9.2 卸载U盘"></a>9.2 卸载U盘</h4><ul>
<li>卸载设备<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">umount &#x2F;mnt&#x2F;usb_flash                               # 普通卸载</span><br><span class="line">lsof | grep &#x2F;mnt&#x2F;usb_flash                          # 查找并杀掉占用进程后卸载</span><br><span class="line">kill -9 [PID]                                       # [PID]为lsof指令查找到的进程ID</span><br><span class="line">umount &#x2F;mnt&#x2F;usb_flash</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h4 id="9-3-格式化U盘"><a href="#9-3-格式化U盘" class="headerlink" title="9.3 格式化U盘"></a>9.3 格式化U盘</h4><ul>
<li>umount卸载分区</li>
<li>fdisk -l查看设备序号</li>
<li>将U盘格式化为FAT32格式<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkfs.vfat -F 32 &#x2F;dev&#x2F;sda1                           # &#x2F;dev&#x2F;sda1为实际待格式化U盘设备</span><br></pre></td></tr></table></figure></li>
<li>将U盘格式化为NTFS格式<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">apt-get install ntfsprogs                           # 安装NTFS读写工具</span><br><span class="line">mkfs.ntfs &#x2F;dev&#x2F;sda1                                 </span><br></pre></td></tr></table></figure></li>
<li>将U盘格式化为Linux EXT格式<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo mkfs.ext4 &#x2F;dev&#x2F;sda1                            # 格式化为ext4分区 </span><br><span class="line">sudo mkfs.ext3 &#x2F;dev&#x2F;sda1                            # 格式化为ext3分区 </span><br><span class="line">sudo mkfs.ext2 &#x2F;dev&#x2F;sda1                            # 格式化为ext2分区 </span><br></pre></td></tr></table></figure>


</li>
</ul>
<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul>
<li><a href="http://blog.csdn.net/kxwinxp/article/details/78370980">树莓派3B+软件源更改</a></li>
<li><a href="http://blog.csdn.net/guyang1995/article/details/53908080">树莓派换源(用的是阿里的源)</a></li>
<li><a href="http://shumeipai.nxez.com/2013/08/31/raspbian-chinese-software-source.html">Raspbian中国软件源</a></li>
<li><a href="https://www.cnblogs.com/edgexie/p/6527992.html">windows远程桌面连接树莓派通过xrdp服务</a></li>
<li><a href="http://shumeipai.nxez.com/2013/09/08/raspberry-pi-to-mount-the-removable-hard-disk.html">给树莓派挂载移动硬盘或U盘</a></li>
</ul>
]]></content>
      <tags>
        <tag>RaspberryPi</tag>
      </tags>
  </entry>
  <entry>
    <title>Python使用cx_Oracle操作Oracle数据库</title>
    <url>/2018/02/13/Python%E4%BD%BF%E7%94%A8cx-Oracle%E6%93%8D%E4%BD%9COracle%E6%95%B0%E6%8D%AE%E5%BA%93/</url>
    <content><![CDATA[<p>本文主要记录Python使用cx_Oracle组件操作Oracle数据库的过程。</p>
<a id="more"></a>

<h2 id="–-gt-Windows"><a href="#–-gt-Windows" class="headerlink" title="–&gt; Windows"></a>–&gt; Windows</h2><h3 id="环境信息"><a href="#环境信息" class="headerlink" title="环境信息"></a>环境信息</h3><ul>
<li>OS：Windows 7 64bit</li>
<li>Python：2.7.14 64bit</li>
<li>cx_Oracle：6.1-cp27-cp27m-win_amd64</li>
<li>instantclient：basic-windows.x64-12.2.0.1.0</li>
</ul>
<h3 id="安装cx-Oracle"><a href="#安装cx-Oracle" class="headerlink" title="安装cx_Oracle"></a>安装cx_Oracle</h3><h4 id="pip安装"><a href="#pip安装" class="headerlink" title="pip安装"></a>pip安装</h4><ul>
<li>使用如下指令安装cx_Oracle<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install cx_Oracle</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="手动安装"><a href="#手动安装" class="headerlink" title="手动安装"></a>手动安装</h4><ul>
<li>打开 <a href="https://pypi.python.org/pypi/cx_Oracle">https://pypi.python.org/pypi/cx_Oracle</a>    </li>
<li>下载cx_Oracle-6.1-cp27-cp27m-win_amd64.whl文件      </li>
<li>使用如下指令安装whl文件<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install cx_Oracle-6.1-cp27-cp27m-win_amd64.whl</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="安装Oralce客户端"><a href="#安装Oralce客户端" class="headerlink" title="安装Oralce客户端"></a>安装Oralce客户端</h3><ul>
<li>打开 <a href="http://www.oracle.com/technetwork/topics/winx64soft-089540.html">http://www.oracle.com/technetwork/topics/winx64soft-089540.html</a>     </li>
<li>下载instantclient-basic-windows.x64-12.2.0.1.0.zip文件   </li>
<li>解压zip文件       </li>
<li>将解压目录添加为环境变量ORACLE_HOME       </li>
<li>将环境变量ORACLE_HOME添加到环境变量Path       </li>
</ul>
<h2 id="–-gt-Linux"><a href="#–-gt-Linux" class="headerlink" title="–&gt; Linux"></a>–&gt; Linux</h2><h3 id="环境信息-1"><a href="#环境信息-1" class="headerlink" title="环境信息"></a>环境信息</h3><ul>
<li>OS：Linux dsic1h1 3.10.0-229.x86_64 </li>
<li>Python：2.7.14 64bit</li>
<li>cx_Oracle：6.1-cp27-cp27m-manylinux1_x86_64.whl</li>
<li>instantclient：basic-linux.x64-12.2.0.1.0</li>
</ul>
<h3 id="安装cx-Oracle-1"><a href="#安装cx-Oracle-1" class="headerlink" title="安装cx_Oracle"></a>安装cx_Oracle</h3><h4 id="pip安装-1"><a href="#pip安装-1" class="headerlink" title="pip安装"></a>pip安装</h4><ul>
<li>使用如下指令安装cx_Oracle<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install cx_Oracle</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="手动安装-1"><a href="#手动安装-1" class="headerlink" title="手动安装"></a>手动安装</h4><ul>
<li>打开 <a href="https://pypi.python.org/pypi/cx_Oracle">https://pypi.python.org/pypi/cx_Oracle</a>    </li>
<li>下载cx_Oracle-6.1-cp27-cp27m-manylinux1_x86_64.whl文件      </li>
<li>上传并使用如下指令安装whl文件<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install cx_Oracle-6.1-cp27-cp27m-manylinux1_x86_64.whl</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="安装Oralce客户端-1"><a href="#安装Oralce客户端-1" class="headerlink" title="安装Oralce客户端"></a>安装Oralce客户端</h3><ul>
<li>打开 <a href="http://www.oracle.com/technetwork/topics/linuxx86-64soft-092277.html">http://www.oracle.com/technetwork/topics/linuxx86-64soft-092277.html</a>     </li>
<li>下载instantclient-basic-linux.x64-12.2.0.1.0.zip文件   </li>
<li>上传并解压zip文件       </li>
<li>新增环境变量LD_LIBRARY_PATH<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 可将以下记录写入环境变量配置文件，如~&#x2F;.bashrc，~&#x2F;.profile</span><br><span class="line"># 其中&#x2F;tmp&#x2F;instantclient_12_2为instantclient解压目录</span><br><span class="line"></span><br><span class="line">export LD_LIBRARY_PATH&#x3D;$&#123;LD_LIBRARY_PATH&#125;:&#x2F;tmp&#x2F;instantclient_12_2       </span><br></pre></td></tr></table></figure>





</li>
</ul>
<h2 id="–-gt-Python下执行测试验证"><a href="#–-gt-Python下执行测试验证" class="headerlink" title="–&gt; Python下执行测试验证"></a>–&gt; Python下执行测试验证</h2><ul>
<li>相关代码如下<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import cx_Oracle  </span><br><span class="line">dsn &#x3D; cx_Oracle.makedsn(&quot;192.168.0.233&quot;,            # 数据库主机IP</span><br><span class="line">                        1521,                       # 数据库服务端口</span><br><span class="line">                        &quot;SID&quot;)                      # 数据库实例SID</span><br><span class="line">conn &#x3D; cx_Oracle.connect(&quot;username&quot;,                # 连接用户</span><br><span class="line">                         &quot;passwd&quot;,                  # 用户密码</span><br><span class="line">                         dsn)                       </span><br><span class="line">curs &#x3D; conn.cursor()  </span><br><span class="line">sql &#x3D; &quot;select * from TBL_NAME where rownum&lt;10&quot;</span><br><span class="line">rr &#x3D; curs.execute(sql)  </span><br><span class="line">row &#x3D; curs.fetchone()  </span><br><span class="line">print row[0]  </span><br><span class="line">curs.close()                                                  </span><br><span class="line">conn.close()  </span><br></pre></td></tr></table></figure>

</li>
</ul>
]]></content>
      <tags>
        <tag>Python</tag>
        <tag>Oracle</tag>
        <tag>cx_Oracle</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows离线安装MySQL5.7.21</title>
    <url>/2018/02/28/Windows%E7%A6%BB%E7%BA%BF%E5%AE%89%E8%A3%85MySQL5-7-21/</url>
    <content><![CDATA[<p>本文主要记录Windows环境下离线安装MySQL数据库过程。</p>
<a id="more"></a>

<h2 id="环境信息"><a href="#环境信息" class="headerlink" title="环境信息"></a>环境信息</h2><ul>
<li>OS：Windows 7 64bit</li>
<li>MySQL：5.7.21</li>
</ul>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><ol>
<li><a href="https://dev.mysql.com/downloads/mysql/5.7.html#downloads">官网</a>下载MySQL压缩文件，选择Windows (x86, 64-bit), ZIP Archive版本。             </li>
<li>解压压缩文件到自定义安装目录，如D:\Program Files，解压之后的MySQL目录为D:\Program Files\mysql-5.7.21-winx64。      </li>
<li>在MySQL目录下新建配置文件my.ini，文件内容如下：</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#########################################################</span><br><span class="line">[mysql] </span><br><span class="line"># MySQL客户端默认字符集 </span><br><span class="line">default-character-set&#x3D;utf8 </span><br><span class="line">[mysqld] </span><br><span class="line"># mysqld服务端口 </span><br><span class="line">port &#x3D; 3306 </span><br><span class="line"># MySQL安装目录，需要根据实际情况填写</span><br><span class="line">basedir&#x3D;D:\Program Files\mysql-5.7.21-winx64            </span><br><span class="line"># MySQL数据库数据存放目录，需要根据实际情况填写</span><br><span class="line">datadir&#x3D;D:\Program Files\mysql-5.7.21-winx64\data       </span><br><span class="line"># 允许最大连接数 </span><br><span class="line">max_connections&#x3D;200 </span><br><span class="line"># 服务端使用的字符集默认为8比特编码的latin1字符集 </span><br><span class="line">character-set-server&#x3D;utf8 </span><br><span class="line"># 创建新表时将使用的默认存储引擎 </span><br><span class="line">default-storage-engine&#x3D;INNODB</span><br><span class="line">#########################################################</span><br></pre></td></tr></table></figure>
<ol start="4">
<li>新增环境变量MYSQL_HOME<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">变量名：MYSQL_HOME</span><br><span class="line">变量值：D:\Program Files\mysql-5.7.21-winx64</span><br></pre></td></tr></table></figure></li>
<li>修改环境变量Path<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">变量名：Path</span><br><span class="line">变量值：在原有Path基础上新增;%MYSQL_HOME%\bin;</span><br></pre></td></tr></table></figure></li>
<li>安装mysqld服务<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysqld install </span><br></pre></td></tr></table></figure></li>
<li>生成data目录<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysqld --initialize-insecure --user&#x3D;mysql       # MySQL 5.7.16后默认不提供data目录</span><br></pre></td></tr></table></figure></li>
<li>启动mysqld服务<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">net start mysql</span><br><span class="line">net stop mysql                                  # 需要关闭mysql服务时执行</span><br></pre></td></tr></table></figure></li>
<li>登陆MySQL<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql -u root -p                                # 首次登录需要设置密码</span><br></pre></td></tr></table></figure>

</li>
</ol>
]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>Database</tag>
        <tag>Manual</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Github Pages创建个人博客</title>
    <url>/2017/11/21/%E4%BD%BF%E7%94%A8Github%20Pages%E5%88%9B%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<p>Hexo为一快速、简洁且高效的博客框架，本文主要记录通过Hexo，借助Github Pages搭建个人博客，并配置主题为NexT的过程。</p>
<a id="more"></a>


<h2 id="软件安装"><a href="#软件安装" class="headerlink" title="软件安装"></a>软件安装</h2><h3 id="安装Git"><a href="#安装Git" class="headerlink" title="安装Git"></a>安装Git</h3><h3 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h3><p>Node.js <a href="https://nodejs.org/en/" title="Node.js下载链接">下载链接</a></p>
<h3 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a>安装Hexo</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br><span class="line">npm install hexo-deployer-git                       # 安装hexo-deployer-git插件</span><br></pre></td></tr></table></figure>




<h2 id="创建及配置Github账号"><a href="#创建及配置Github账号" class="headerlink" title="创建及配置Github账号"></a>创建及配置Github账号</h2><h3 id="创建Github账号"><a href="#创建Github账号" class="headerlink" title="创建Github账号"></a>创建Github账号</h3><p>访问Github官网进行注册</p>
<h3 id="创建GitPages"><a href="#创建GitPages" class="headerlink" title="创建GitPages"></a>创建GitPages</h3><p>即创建与账号 <strong>同名</strong> 的Repository，例GitHub帐号是yourid，则新建的Repository名称应为yourid.github.io<br>创建之后可浏览器访问yourid.github**.io**，如果能够正常访问，则证明GitPages创建成功</p>
<h3 id="配置SSH信息"><a href="#配置SSH信息" class="headerlink" title="配置SSH信息"></a>配置SSH信息</h3><h4 id="生成SSH-Key"><a href="#生成SSH-Key" class="headerlink" title="生成SSH Key"></a>生成SSH Key</h4><p>本地生成一个SSH Key</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C &quot;Email&quot;                        # Email为GitHub的注册邮箱</span><br></pre></td></tr></table></figure>
<p>根据页面提示输入相关信息，可以连续回车采取默认配置。<br>最终生成结果如下，*.pub即为公钥文件。</p>
<h4 id="复制公钥内容到Github账户信息中"><a href="#复制公钥内容到Github账户信息中" class="headerlink" title="复制公钥内容到Github账户信息中"></a>复制公钥内容到Github账户信息中</h4><ul>
<li>打开~/.ssh/id_rsa.pub文件，复制里面的内容，如下</li>
<li>打开Github官网，登录后进入到个人设置(点击头像-&gt;setting)</li>
<li>点击右侧的SSH Keys，点击Add SSH key</li>
<li>填写title之后，将之前复制的内容粘贴到Key框中</li>
<li>点击Add key提交</li>
</ul>
<h4 id="测试SSH是否配置成功"><a href="#测试SSH是否配置成功" class="headerlink" title="测试SSH是否配置成功"></a>测试SSH是否配置成功</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 本地执行如下代码</span><br><span class="line">ssh -T git@github.com</span><br><span class="line">Hi yourid! You&#39;ve successfully authenticated, but GitHub does not provide shell access.</span><br><span class="line"># 如提示以上信息，则证明ssh配置成功</span><br></pre></td></tr></table></figure>


<h4 id="配置Github-账户"><a href="#配置Github-账户" class="headerlink" title="配置Github 账户"></a>配置Github 账户</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 本地执行如下代码</span><br><span class="line">git config --global user.name &quot;Username&quot;            # Username为GitHub的注册用户名</span><br><span class="line">git config --global user.email &quot;Email&quot;              # Email为GitHub的注册邮箱</span><br><span class="line">git config --list                                   # 查看已设配置(Username,Email)是否正确     </span><br></pre></td></tr></table></figure>




<h2 id="Hexo建站"><a href="#Hexo建站" class="headerlink" title="Hexo建站"></a>Hexo建站</h2><h3 id="使用Hexo进行本地建站"><a href="#使用Hexo进行本地建站" class="headerlink" title="使用Hexo进行本地建站"></a>使用Hexo进行本地建站</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 本地执行如下代码</span><br><span class="line">cd C:&#x2F;hexo                          # 切换到本地目录</span><br><span class="line">hexo init C:&#x2F;hexo                   # 进行Hexo初始化</span><br><span class="line">npm install                         # 安装</span><br></pre></td></tr></table></figure>
<p>如果Hexo安装成功，则在本地Hexo目录下的目录结构如下，详细文件含义见<a href="https://hexo.io/zh-cn/docs/setup.html">Hexo官方文档</a></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── _config.yml                     # 网站配置文件，可在此配置大部分的参数</span><br><span class="line">├── package.json </span><br><span class="line">├── scaffolds                       # 模板文件存放路径。新增文章时Hexo会根据scaffold创建文件</span><br><span class="line">├── source                          # 用户资源存放路径</span><br><span class="line">|   ├── _drafts</span><br><span class="line">|   └── _posts</span><br><span class="line">└── themes                          # 网站主题存放路径。Hexo会根据主题来生成静态页面</span><br></pre></td></tr></table></figure>
<p>本地启动默认的静态网站。访问地址为：<a href="http://localhost:4000/">http://localhost:4000/</a></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo server                         # 本地启动</span><br></pre></td></tr></table></figure>



<h3 id="部署网站"><a href="#部署网站" class="headerlink" title="部署网站"></a>部署网站</h3><p>打开本地Hexo目录中的_config.yml文件，找到如下配置并根据注释说明更新</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Deployment</span><br><span class="line">## Docs: http:&#x2F;&#x2F;hexo.io&#x2F;docs&#x2F;deployment.html</span><br><span class="line">deploy: </span><br><span class="line">  type: git                                         # 在yml文件中，:后面需要要带空格 </span><br><span class="line">  repo: git@github.com:yourid&#x2F;yourid.github.io      # 其中yourid为GitHub的注册用户名  </span><br></pre></td></tr></table></figure>



<h3 id="将网站部署到GitPages"><a href="#将网站部署到GitPages" class="headerlink" title="将网站部署到GitPages"></a>将网站部署到GitPages</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ cd c:&#x2F;hexo                                        # 进入网站根目录</span><br><span class="line">$ hexo generate                                     # 编译本地内容并生成静态文件</span><br><span class="line">$ hexo deploy                                       # 部署静态网页文件到GitHub</span><br></pre></td></tr></table></figure>
<p>此时，通过访问 <a href="http://yourid.github.io/">http://yourid.github.io</a> 可以看到默认的Hexo首页        </p>
<h2 id="更新Hexo主题为NexT"><a href="#更新Hexo主题为NexT" class="headerlink" title="更新Hexo主题为NexT"></a>更新Hexo主题为NexT</h2><h3 id="下载NexT主题"><a href="#下载NexT主题" class="headerlink" title="下载NexT主题"></a>下载NexT主题</h3><p><a href="https://github.com/iissnan/hexo-theme-next/releases">5.1.1版本下载地址</a><br>下载后解压文件，重命名为next，拷贝到C:\hexo\themes 目录</p>
<h3 id="修改网站的主题为NexT"><a href="#修改网站的主题为NexT" class="headerlink" title="修改网站的主题为NexT"></a>修改网站的主题为NexT</h3><p>打开C:\Hexo下的_config.yml文件，按照如下配置，找到theme字段，将其修改为next   </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Extensions</span><br><span class="line">## Plugins: http:&#x2F;&#x2F;hexo.io&#x2F;plugins&#x2F;</span><br><span class="line">## Themes: http:&#x2F;&#x2F;hexo.io&#x2F;themes&#x2F;</span><br><span class="line">theme: next</span><br></pre></td></tr></table></figure>



<h3 id="本地验证是否可用"><a href="#本地验证是否可用" class="headerlink" title="本地验证是否可用"></a>本地验证是否可用</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo server --debug</span><br></pre></td></tr></table></figure>
<p>访问本地网站<a href="http://localhost:4000/%EF%BC%8C%E7%A1%AE%E8%AE%A4%E7%BD%91%E7%AB%99%E4%B8%BB%E9%A2%98%E6%98%AF%E5%90%A6%E5%88%87%E6%8D%A2%E4%B8%BANexT%E3%80%82">http://localhost:4000/，确认网站主题是否切换为NexT。</a></p>
<h3 id="提交更新到Github"><a href="#提交更新到Github" class="headerlink" title="提交更新到Github"></a>提交更新到Github</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ cd c:&#x2F;hexo                                        # 进入网站根目录</span><br><span class="line">$ hexo generate                                     # 编译本地内容并生成静态文件</span><br><span class="line">$ hexo deploy                                       # 部署静态网页文件到GitHub</span><br></pre></td></tr></table></figure>



<h2 id="Hexo其他操作"><a href="#Hexo其他操作" class="headerlink" title="Hexo其他操作"></a>Hexo其他操作</h2><h3 id="发布文章"><a href="#发布文章" class="headerlink" title="发布文章"></a>发布文章</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo n &quot;postname&quot;                                   # 新增文章</span><br></pre></td></tr></table></figure>
<p>命令执行后，会在Hexo的source_post文件夹下新建一个名为”postname.md”的文件，也可以直接在Hexo的source_post文件夹下新建.md结尾的文件。新增文件后重新进行编译和部署以在GitHub Pages上生效。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ hexo generate                                     # 编译本地内容并生成静态文件</span><br><span class="line">$ hexo deploy                                       # 部署静态网页文件到GitHub</span><br></pre></td></tr></table></figure>



<h3 id="标签功能"><a href="#标签功能" class="headerlink" title="标签功能"></a>标签功能</h3><h3 id="搜索功能"><a href="#搜索功能" class="headerlink" title="搜索功能"></a>搜索功能</h3><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><ul>
<li>Hexo详细操作，可参考<a href="https://hexo.io/zh-cn/docs/">Hexo参考文档</a>    </li>
<li>NexT主题细节配置，可参考<a href="http://theme-next.iissnan.com/getting-started.html">NexT 使用文档</a></li>
<li>Markdown文章书写，可参考<a href="https://studygolang.com/markdown">Markdown简明教程</a></li>
<li>Hexo NexT主题中集成<a href="https://asdfv1929.github.io/2018/01/20/gitalk/">gitalk评论系统</a></li>
<li>Hexo NexT主题中集成<a href="https://www.jianshu.com/p/02fc71f3633f">gitalk评论系统</a></li>
</ul>
]]></content>
      <tags>
        <tag>Git</tag>
        <tag>Github</tag>
        <tag>GithubPages</tag>
      </tags>
  </entry>
  <entry>
    <title>动态图解十大经典排序算法</title>
    <url>/2020/03/28/%E5%8A%A8%E6%80%81%E5%9B%BE%E8%A7%A3%E5%8D%81%E5%A4%A7%E7%BB%8F%E5%85%B8%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>动态图解十大经典排序算法</p>
<a id="more"></a>

<h4 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h4><p><img src="/images/SortAlgo_QuickSort.gif" alt="&quot;QuickSort&quot;"></p>
]]></content>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>我的利器</title>
    <url>/2018/10/28/%E6%88%91%E7%9A%84%E5%88%A9%E5%99%A8/</url>
    <content><![CDATA[<p>一直都很喜欢 <strong><a href="http://liqi.io/creators/" title="利器">利器</a></strong> 这个项目，在这里分享一下我自己的利器吧。</p>
<a id="more"></a>

<h4 id="介绍一下你自己和所做的工作。"><a href="#介绍一下你自己和所做的工作。" class="headerlink" title="介绍一下你自己和所做的工作。"></a>介绍一下你自己和所做的工作。</h4><p>我希望能够在和别人做自我介绍的时候，可以说自己是一名软件研发工程师。</p>
<h4 id="你都在使用哪些硬件？"><a href="#你都在使用哪些硬件？" class="headerlink" title="你都在使用哪些硬件？"></a>你都在使用哪些硬件？</h4><ul>
<li><p>ThinkPad X1 Carbon 2018<br>公司配的电脑，我喜欢自己的生活中只有一台电脑，我不排斥工作和生活混在一起。</p>
</li>
<li><p>iPhone 5s<br>现在已经是2018年10月，我还在用iPhone 5s 16G。这是我的第二部手机，已经陪伴我5年多了，前段时间第二次换了电池。因为平时的重心都在电脑上，所以并不会感觉这部手机不够用。</p>
</li>
<li><p>FILCO忍者二代机械键盘<br>红轴，双模，无刻，唯一的遗憾是当初犹豫再三之后还是选了104键的版本。现在想想，无论是对于有限的桌面空间，还是为了方便携带，87键都是一个更好的选择。小键盘输入数字的确更加方便，但是我对这个的需求其实并没有当初想的那么强烈，尤其是在已经习惯了笔记本的布局之后。</p>
</li>
<li><p>Kindle Paperwhite<br>间歇性疯狂读书。</p>
</li>
<li><p>Sony运动耳机 (NW-WS414)<br>女朋友送的第一个生日礼物。</p>
</li>
</ul>
<ul>
<li>Raspberry Pi 3B<br>当初心心念念，现在因为找不到刚需使用场景而吃灰中。</li>
</ul>
<h4 id="软件呢？"><a href="#软件呢？" class="headerlink" title="软件呢？"></a>软件呢？</h4><ul>
<li>Chrome</li>
<li>cmder</li>
<li>Notepad++</li>
<li>SublimeText3</li>
<li>VSCode</li>
<li>IntelliJ IDEA</li>
<li>SecureCRT</li>
<li>Everything</li>
<li>BeyondCompare</li>
<li>Navicat Premium 12</li>
<li>Bandizip</li>
<li>Postman</li>
<li>Shadowsocks</li>
<li>SpeedPan</li>
<li>FSCapture</li>
</ul>
<h4 id="你最理想的工作环境是什么？"><a href="#你最理想的工作环境是什么？" class="headerlink" title="你最理想的工作环境是什么？"></a>你最理想的工作环境是什么？</h4><p>曾经有一天，突然觉得美剧<Silicon Valley>中，Richard他们一起工作生活的那个House就是我最渴望的工作环境。<br><img src="/images/siliconvalley.jpg" alt="&quot;siliconvalley&quot;"></p>
<h4 id="你平时获得工作灵感的方式有哪些？"><a href="#你平时获得工作灵感的方式有哪些？" class="headerlink" title="你平时获得工作灵感的方式有哪些？"></a>你平时获得工作灵感的方式有哪些？</h4><p>逛<a href="https://www.zhihu.com/">知乎</a>，<a href="https://www.v2ex.com/">V2EX</a>。有时候会觉得，可能我一直都未曾真正有过灵感。</p>
<h4 id="推荐一件生活中的利器给大家。"><a href="#推荐一件生活中的利器给大家。" class="headerlink" title="推荐一件生活中的利器给大家。"></a>推荐一件生活中的利器给大家。</h4><p>身边越来越多的人开始切换到了Windows10，每次看到别人任务栏上不曾使却又存在着的搜索框，我就会在心里小小的“鄙视”一下这个人。__<a href="https://jingyan.baidu.com/article/a378c960a7f625b32828303d.html">单击三次鼠标即可关闭</a>__，不要让它再占用你“寸土寸金”的任务栏啦！<br>还有将键盘上的CapsLock键映射为Ctrl键，相信我，世界从此不同。</p>
]]></content>
      <tags>
        <tag>程序员的自我修养</tag>
      </tags>
  </entry>
  <entry>
    <title>树莓派使用bypy实现百度网盘上传下载</title>
    <url>/2018/03/19/%E6%A0%91%E8%8E%93%E6%B4%BE%E4%BD%BF%E7%94%A8%E7%99%BE%E5%BA%A6%E7%BD%91%E7%9B%98%E5%AE%9E%E7%8E%B0%E4%B8%8A%E4%BC%A0%E4%B8%8B%E8%BD%BD/</url>
    <content><![CDATA[<p>bypy是百度网盘的Python客户端，本文主要记录在树莓派上使用bypy实现百度网盘上传下载配置过程。</p>
<a id="more"></a>

<h2 id="前提条件"><a href="#前提条件" class="headerlink" title="前提条件"></a>前提条件</h2><ul>
<li>已安装Python</li>
<li>已安装requests类库(sudo pip install requests)</li>
</ul>
<h2 id="安装bypy"><a href="#安装bypy" class="headerlink" title="安装bypy"></a>安装bypy</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo pip install bypy                                       # 安装bypy</span><br></pre></td></tr></table></figure>

<h2 id="验证bypy并授权"><a href="#验证bypy并授权" class="headerlink" title="验证bypy并授权"></a>验证bypy并授权</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo bypy.py info                                           # 验证bypy并授权</span><br><span class="line">python -m bypy info</span><br></pre></td></tr></table></figure>
<p>复制”Please Visit:”下的网址，登陆百度账号，并复制授权码。<br>授权成功后会看到授权网盘的总容量和已使用容量等信息。</p>
<h2 id="bypy相关指令"><a href="#bypy相关指令" class="headerlink" title="bypy相关指令"></a>bypy相关指令</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bypy.py upload                                              # 把当前目录同步到云盘</span><br><span class="line">bypy.py downdir                                             # 把云盘内容同步到本地来</span><br><span class="line">bypy.py syncdown                                            # 把云盘内容同步到本地来</span><br><span class="line">bypy.py compare                                             # 比较本地当前目录和云盘根目录</span><br></pre></td></tr></table></figure>

<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><ul>
<li>百度网盘下bypy同步路径：/我的应用数据/bypy</li>
</ul>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul>
<li><a href="https://github.com/houtianze/bypy">bypy Github</a></li>
<li><a href="https://www.cnblogs.com/xiaowuyi/p/4160397.html">利用bypy实现树莓派NAS同步百度云</a></li>
</ul>
]]></content>
      <tags>
        <tag>RaspberryPi</tag>
        <tag>百度网盘</tag>
      </tags>
  </entry>
  <entry>
    <title>[转载]Java泛型详解</title>
    <url>/2018/09/29/Java%E6%B3%9B%E5%9E%8B%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>泛型是Java中一个非常重要的知识点，在Java集合类框架中泛型被广泛应用。本文我们将从零开始来看一下Java泛型的设计，将会涉及到通配符处理，以及让人苦恼的类型擦除。<br>原文地址 <a href="http://www.ziwenxie.site/2017/03/01/java-generic/">ZiWenXie</a></p>
<a id="more"></a>


<h2 id="泛型基础"><a href="#泛型基础" class="headerlink" title="泛型基础"></a>泛型基础</h2><h3 id="泛型类"><a href="#泛型类" class="headerlink" title="泛型类"></a>泛型类</h3><p>我们首先定义一个简单的Box类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Box</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String object;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(String object)</span> </span>&#123; <span class="keyword">this</span>.object = object; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">get</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> object; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是最常见的做法，这样做的一个坏处是Box里面现在只能装入String类型的元素，今后如果我们需要装入Integer等其他类型的元素，还必须要另外重写一个Box，代码得不到复用，使用泛型可以很好的解决这个问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Box</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="comment">// T stands for &quot;Type&quot;</span></span><br><span class="line">    <span class="keyword">private</span> T t;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(T t)</span> </span>&#123; <span class="keyword">this</span>.t = t; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">get</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> t; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样我们的Box类便可以得到复用，我们可以将T替换成任何我们想要的类型：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Box&lt;Integer&gt; integerBox = <span class="keyword">new</span> Box&lt;Integer&gt;();</span><br><span class="line">Box&lt;Double&gt; doubleBox = <span class="keyword">new</span> Box&lt;Double&gt;();</span><br><span class="line">Box&lt;String&gt; stringBox = <span class="keyword">new</span> Box&lt;String&gt;();</span><br></pre></td></tr></table></figure>
<h3 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Util</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;K, V&gt; <span class="function"><span class="keyword">boolean</span> <span class="title">compare</span><span class="params">(Pair&lt;K, V&gt; p1, Pair&lt;K, V&gt; p2)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> p1.getKey().equals(p2.getKey()) &amp;&amp;</span><br><span class="line">               p1.getValue().equals(p2.getValue());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Pair</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> K key;</span><br><span class="line">    <span class="keyword">private</span> V value;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Pair</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.key = key;</span><br><span class="line">        <span class="keyword">this</span>.value = value;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setKey</span><span class="params">(K key)</span> </span>&#123; <span class="keyword">this</span>.key = key; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setValue</span><span class="params">(V value)</span> </span>&#123; <span class="keyword">this</span>.value = value; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> K <span class="title">getKey</span><span class="params">()</span>   </span>&#123; <span class="keyword">return</span> key; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> V <span class="title">getValue</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> value; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以像下面这样去调用泛型方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;Integer, String&gt; p1 = <span class="keyword">new</span> Pair&lt;&gt;(<span class="number">1</span>, <span class="string">&quot;apple&quot;</span>);</span><br><span class="line">Pair&lt;Integer, String&gt; p2 = <span class="keyword">new</span> Pair&lt;&gt;(<span class="number">2</span>, <span class="string">&quot;pear&quot;</span>);</span><br><span class="line"><span class="keyword">boolean</span> same = Util.&lt;Integer, String&gt;compare(p1, p2);</span><br></pre></td></tr></table></figure>
<p>或者在Java1.7/1.8利用type inference，让Java自动推导出相应的类型参数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;Integer, String&gt; p1 = <span class="keyword">new</span> Pair&lt;&gt;(<span class="number">1</span>, <span class="string">&quot;apple&quot;</span>);</span><br><span class="line">Pair&lt;Integer, String&gt; p2 = <span class="keyword">new</span> Pair&lt;&gt;(<span class="number">2</span>, <span class="string">&quot;pear&quot;</span>);</span><br><span class="line"><span class="keyword">boolean</span> same = Util.compare(p1, p2);</span><br></pre></td></tr></table></figure>
<h3 id="边界符"><a href="#边界符" class="headerlink" title="边界符"></a>边界符</h3><p>现在我们要实现这样一个功能，查找一个泛型数组中大于某个特定元素的个数，我们可以这样实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">int</span> <span class="title">countGreaterThan</span><span class="params">(T[] anArray, T elem)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (T e : anArray)</span><br><span class="line">        <span class="keyword">if</span> (e &gt; elem)  <span class="comment">// compiler error</span></span><br><span class="line">            ++count;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但是这样很明显是错误的，因为除了short, int, double, long, float, byte, char等原始类型，其他的类并不一定能使用操作符&gt;，所以编译器报错，那怎么解决这个问题呢？答案是使用边界符。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Comparable</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(T o)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>做一个类似于下面这样的声明，这样就等于告诉编译器类型参数T代表的都是实现了Comparable接口的类，这样等于告诉编译器它们都至少实现了compareTo方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T extends Comparable&lt;T&gt;&gt; <span class="function"><span class="keyword">int</span> <span class="title">countGreaterThan</span><span class="params">(T[] anArray, T elem)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (T e : anArray)</span><br><span class="line">        <span class="keyword">if</span> (e.compareTo(elem) &gt; <span class="number">0</span>)</span><br><span class="line">            ++count;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h2><p>在了解通配符之前，我们首先必须要澄清一个概念，还是借用我们上面定义的Box类，假设我们添加一个这样的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">boxTest</span><span class="params">(Box&lt;Number&gt; n)</span> </span>&#123; <span class="comment">/* ... */</span> &#125;</span><br></pre></td></tr></table></figure>
<p>那么现在Box<Number> n允许接受什么类型的参数？我们是否能够传入Box<Integer>或者Box<Double>呢？答案是否定的，虽然Integer和Double是Number的子类，但是在泛型中Box<Integer>或者Box<Double>与Box<Number>之间并没有任何的关系。这一点非常重要，接下来我们通过一个完整的例子来加深一下理解。</p>
<p>首先我们先定义几个简单的类，下面我们将用到它：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Fruit</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Apple</span> <span class="keyword">extends</span> <span class="title">Fruit</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Orange</span> <span class="keyword">extends</span> <span class="title">Fruit</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>
<p>下面这个例子中，我们创建了一个泛型类Reader，然后在f1()中当我们尝试Fruit f = fruitReader.readExact(apples);编译器会报错，因为List<Fruit>与List<Apple>之间并没有任何的关系。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericReading</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> List&lt;Apple&gt; apples = Arrays.asList(<span class="keyword">new</span> Apple());</span><br><span class="line">    <span class="keyword">static</span> List&lt;Fruit&gt; fruit = Arrays.asList(<span class="keyword">new</span> Fruit());</span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Reader</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">        <span class="function">T <span class="title">readExact</span><span class="params">(List&lt;T&gt; list)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> list.get(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">f1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Reader&lt;Fruit&gt; fruitReader = <span class="keyword">new</span> Reader&lt;Fruit&gt;();</span><br><span class="line">        <span class="comment">// Errors: List&lt;Fruit&gt; cannot be applied to List&lt;Apple&gt;.</span></span><br><span class="line">        <span class="comment">// Fruit f = fruitReader.readExact(apples);</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        f1();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但是按照我们通常的思维习惯，Apple和Fruit之间肯定是存在联系，然而编译器却无法识别，那怎么在泛型代码中解决这个问题呢？我们可以通过使用通配符来解决这个问题：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CovariantReader</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function">T <span class="title">readCovariant</span><span class="params">(List&lt;? extends T&gt; list)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> list.get(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">f2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    CovariantReader&lt;Fruit&gt; fruitReader = <span class="keyword">new</span> CovariantReader&lt;Fruit&gt;();</span><br><span class="line">    Fruit f = fruitReader.readCovariant(fruit);</span><br><span class="line">    Fruit a = fruitReader.readCovariant(apples);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    f2();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样就相当与告诉编译器， fruitReader的readCovariant方法接受的参数只要是满足Fruit的子类就行(包括Fruit自身)，这样子类和父类之间的关系也就关联上了。</p>
<h2 id="PECS原则"><a href="#PECS原则" class="headerlink" title="PECS原则"></a>PECS原则</h2><p>上面我们看到了类似&lt;? extends T&gt;的用法，利用它我们可以从list里面get元素，那么我们可不可以往list里面add元素呢？我们来尝试一下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericsAndCovariance</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Wildcards allow covariance:</span></span><br><span class="line">        List&lt;? extends Fruit&gt; flist = <span class="keyword">new</span> ArrayList&lt;Apple&gt;();</span><br><span class="line">        <span class="comment">// Compile Error: can&#x27;t add any type of object:</span></span><br><span class="line">        <span class="comment">// flist.add(new Apple())</span></span><br><span class="line">        <span class="comment">// flist.add(new Orange())</span></span><br><span class="line">        <span class="comment">// flist.add(new Fruit())</span></span><br><span class="line">        <span class="comment">// flist.add(new Object())</span></span><br><span class="line">        flist.add(<span class="keyword">null</span>); <span class="comment">// Legal but uninteresting</span></span><br><span class="line">        <span class="comment">// We Know that it returns at least Fruit:</span></span><br><span class="line">        Fruit f = flist.get(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>答案是否定，Java编译器不允许我们这样做，为什么呢？对于这个问题我们不妨从编译器的角度去考虑。因为List&lt;? extends Fruit&gt; flist它自身可以有多种含义：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;? extends Fruit&gt; flist = <span class="keyword">new</span> ArrayList&lt;Fruit&gt;();</span><br><span class="line">List&lt;? extends Fruit&gt; flist = <span class="keyword">new</span> ArrayList&lt;Apple&gt;();</span><br><span class="line">List&lt;? extends Fruit&gt; flist = <span class="keyword">new</span> ArrayList&lt;Orange&gt;();</span><br></pre></td></tr></table></figure>

<p>当我们尝试add一个Apple的时候，flist可能指向new ArrayList<Orange>();<br>当我们尝试add一个Orange的时候，flist可能指向new ArrayList<Apple>();<br>当我们尝试add一个Fruit的时候，这个Fruit可以是任何类型的Fruit，而flist可能只想某种特定类型的Fruit，编译器无法识别所以会报错。</p>
<p>所以对于实现了&lt;? extends T&gt;的集合类只能将它视为Producer向外提供(get)元素，而不能作为Consumer来对外获取(add)元素。<br>如果我们要add元素应该怎么做呢？可以使用&lt;? super T&gt;：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericWriting</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> List&lt;Apple&gt; apples = <span class="keyword">new</span> ArrayList&lt;Apple&gt;();</span><br><span class="line">    <span class="keyword">static</span> List&lt;Fruit&gt; fruit = <span class="keyword">new</span> ArrayList&lt;Fruit&gt;();</span><br><span class="line">    <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">writeExact</span><span class="params">(List&lt;T&gt; list, T item)</span> </span>&#123;</span><br><span class="line">        list.add(item);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">f1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        writeExact(apples, <span class="keyword">new</span> Apple());</span><br><span class="line">        writeExact(fruit, <span class="keyword">new</span> Apple());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">writeWithWildcard</span><span class="params">(List&lt;? <span class="keyword">super</span> T&gt; list, T item)</span> </span>&#123;</span><br><span class="line">        list.add(item)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">f2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        writeWithWildcard(apples, <span class="keyword">new</span> Apple());</span><br><span class="line">        writeWithWildcard(fruit, <span class="keyword">new</span> Apple());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        f1(); f2();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样我们可以往容器里面添加元素了，但是使用super的坏处是以后不能get容器里面的元素了，原因很简单，我们继续从编译器的角度考虑这个问题，对于List&lt;? super Apple&gt; list，它可以有下面几种含义：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;? <span class="keyword">super</span> Apple&gt; list = <span class="keyword">new</span> ArrayList&lt;Apple&gt;();</span><br><span class="line">List&lt;? <span class="keyword">super</span> Apple&gt; list = <span class="keyword">new</span> ArrayList&lt;Fruit&gt;();</span><br><span class="line">List&lt;? <span class="keyword">super</span> Apple&gt; list = <span class="keyword">new</span> ArrayList&lt;Object&gt;();</span><br></pre></td></tr></table></figure>
<p>当我们尝试通过list来get一个Apple的时候，可能会get得到一个Fruit，这个Fruit可以是Orange等其他类型的Fruit。<br>根据上面的例子，我们可以总结出一条规律，”Producer Extends, Consumer Super”：<br>“Producer Extends” – 如果你需要一个只读List，用它来produce T，那么使用? extends T。<br>“Consumer Super” – 如果你需要一个只写List，用它来consume T，那么使用? super T。<br>如果需要同时读取以及写入，那么我们就不能使用通配符了。<br>如何阅读过一些Java集合类的源码，可以发现通常我们会将两者结合起来一起用，比如像下面这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Collections</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">copy</span><span class="params">(List&lt;? <span class="keyword">super</span> T&gt; dest, List&lt;? extends T&gt; src)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;src.size(); i++)</span><br><span class="line">            dest.set(i, src.get(i));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="类型擦除"><a href="#类型擦除" class="headerlink" title="类型擦除"></a>类型擦除</h2><p>Java泛型中最令人苦恼的地方或许就是类型擦除了，特别是对于有C++经验的程序员。类型擦除就是说Java泛型只能用于在编译期间的静态类型检查，然后编译器生成的代码会擦除相应的类型信息，这样到了运行期间实际上JVM根本就知道泛型所代表的具体类型。这样做的目的是因为Java泛型是1.5之后才被引入的，为了保持向下的兼容性，所以只能做类型擦除来兼容以前的非泛型代码。对于这一点，如果阅读Java集合框架的源码，可以发现有些类其实并不支持泛型。</p>
<p>说了这么多，那么泛型擦除到底是什么意思呢？我们先来看一下下面这个简单的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T data;</span><br><span class="line">    <span class="keyword">private</span> Node&lt;T&gt; next;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(T data, Node&lt;T&gt; next)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">        <span class="keyword">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getData</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> data; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译器做完相应的类型检查之后，实际上到了运行期间上面这段代码实际上将转换成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Object data;</span><br><span class="line">    <span class="keyword">private</span> Node next;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(Object data, Node next)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">        <span class="keyword">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">getData</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> data; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这意味着不管我们声明Node<String>还是Node<Integer>，到了运行期间，JVM统统视为Node<Object>。有没有什么办法可以解决这个问题呢？这就需要我们自己重新设置bounds了，将上面的代码修改成下面这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">Comparable</span>&lt;<span class="title">T</span>&gt;&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T data;</span><br><span class="line">    <span class="keyword">private</span> Node&lt;T&gt; next;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(T data, Node&lt;T&gt; next)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">        <span class="keyword">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getData</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> data; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样编译器就会将T出现的地方替换成Comparable而不再是默认的Object了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Comparable data;</span><br><span class="line">    <span class="keyword">private</span> Node next;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(Comparable data, Node next)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">        <span class="keyword">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Comparable <span class="title">getData</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> data; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面的概念或许还是比较好理解，但其实泛型擦除带来的问题远远不止这些，接下来我们系统地来看一下类型擦除所带来的一些问题，有些问题在C++的泛型中可能不会遇见，但是在Java中却需要格外小心。</p>
<h3 id="问题一"><a href="#问题一" class="headerlink" title="问题一"></a>问题一</h3><p>在Java中不允许创建泛型数组，类似下面这样的做法编译器会报错：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt;[] arrayOfLists = <span class="keyword">new</span> List&lt;Integer&gt;[<span class="number">2</span>];  <span class="comment">// compile-time error</span></span><br></pre></td></tr></table></figure>
<p>为什么编译器不支持上面这样的做法呢？继续使用逆向思维，我们站在编译器的角度来考虑这个问题。</p>
<p>我们先来看一下下面这个例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object[] strings = <span class="keyword">new</span> String[<span class="number">2</span>];</span><br><span class="line">strings[<span class="number">0</span>] = <span class="string">&quot;hi&quot;</span>;   <span class="comment">// OK</span></span><br><span class="line">strings[<span class="number">1</span>] = <span class="number">100</span>;    <span class="comment">// An ArrayStoreException is thrown.</span></span><br></pre></td></tr></table></figure>
<p>对于上面这段代码还是很好理解，字符串数组不能存放整型元素，而且这样的错误往往要等到代码运行的时候才能发现，编译器是无法识别的。接下来我们再来看一下假设Java支持泛型数组的创建会出现什么后果：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object[] stringLists = <span class="keyword">new</span> List&lt;String&gt;[];  <span class="comment">// compiler error, but pretend it&#x27;s allowed</span></span><br><span class="line">stringLists[<span class="number">0</span>] = <span class="keyword">new</span> ArrayList&lt;String&gt;();   <span class="comment">// OK</span></span><br><span class="line"><span class="comment">// An ArrayStoreException should be thrown, but the runtime can&#x27;t detect it.</span></span><br><span class="line">stringLists[<span class="number">1</span>] = <span class="keyword">new</span> ArrayList&lt;Integer&gt;();</span><br></pre></td></tr></table></figure>
<p>假设我们支持泛型数组的创建，由于运行时期类型信息已经被擦除，JVM实际上根本就不知道new ArrayList<String>()和new ArrayList<Integer>()的区别。类似这样的错误假如出现才实际的应用场景中，将非常难以察觉。<br>如果你对上面这一点还抱有怀疑的话，可以尝试运行下面这段代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ErasedTypeEquivalence</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Class c1 = <span class="keyword">new</span> ArrayList&lt;String&gt;().getClass();</span><br><span class="line">        Class c2 = <span class="keyword">new</span> ArrayList&lt;Integer&gt;().getClass();</span><br><span class="line">        System.out.println(c1 == c2); <span class="comment">// true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h3 id="问题二"><a href="#问题二" class="headerlink" title="问题二"></a>问题二</h3><p>继续复用我们上面的Node的类，对于泛型代码，Java编译器实际上还会偷偷帮我们实现一个Bridge method。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> T data;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(T data)</span> </span>&#123; <span class="keyword">this</span>.data = data; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setData</span><span class="params">(T data)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Node.setData&quot;</span>);</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyNode</span> <span class="keyword">extends</span> <span class="title">Node</span>&lt;<span class="title">Integer</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyNode</span><span class="params">(Integer data)</span> </span>&#123; <span class="keyword">super</span>(data); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setData</span><span class="params">(Integer data)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;MyNode.setData&quot;</span>);</span><br><span class="line">        <span class="keyword">super</span>.setData(data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看完上面的分析之后，你可能会认为在类型擦除后，编译器会将Node和MyNode变成下面这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> Object data;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Node</span><span class="params">(Object data)</span> </span>&#123; <span class="keyword">this</span>.data = data; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setData</span><span class="params">(Object data)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Node.setData&quot;</span>);</span><br><span class="line">        <span class="keyword">this</span>.data = data;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyNode</span> <span class="keyword">extends</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyNode</span><span class="params">(Integer data)</span> </span>&#123; <span class="keyword">super</span>(data); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setData</span><span class="params">(Integer data)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;MyNode.setData&quot;</span>);</span><br><span class="line">        <span class="keyword">super</span>.setData(data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际上不是这样的，我们先来看一下下面这段代码，这段代码运行的时候会抛出ClassCastException异常，提示String无法转换成Integer：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">MyNode mn = <span class="keyword">new</span> MyNode(<span class="number">5</span>);</span><br><span class="line">Node n = mn; <span class="comment">// A raw type - compiler throws an unchecked warning</span></span><br><span class="line">n.setData(<span class="string">&quot;Hello&quot;</span>); <span class="comment">// Causes a ClassCastException to be thrown.</span></span><br><span class="line"><span class="comment">// Integer x = mn.data;</span></span><br></pre></td></tr></table></figure>
<p>如果按照我们上面生成的代码，运行到第3行的时候不应该报错(注意我注释掉了第4行)，因为MyNode中不存在setData(String data)方法，所以只能调用父类Node的setData(Object data)方法，既然这样上面的第3行代码不应该报错，因为String当然可以转换成Object了，那ClassCastException到底是怎么抛出的？</p>
<p>实际上Java编译器对上面代码自动还做了一个处理：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyNode</span> <span class="keyword">extends</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Bridge method generated by the compiler</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setData</span><span class="params">(Object data)</span> </span>&#123;</span><br><span class="line">        setData((Integer) data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setData</span><span class="params">(Integer data)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;MyNode.setData&quot;</span>);</span><br><span class="line">        <span class="keyword">super</span>.setData(data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这也就是为什么上面会报错的原因了，setData((Integer) data);的时候String无法转换成Integer。所以上面第2行编译器提示unchecked warning的时候，我们不能选择忽略，不然要等到运行期间才能发现异常。如果我们一开始加上Node<Integer> n = mn就好了，这样编译器就可以提前帮我们发现错误。</p>
<h3 id="问题三"><a href="#问题三" class="headerlink" title="问题三"></a>问题三</h3><p>正如我们上面提到的，Java泛型很大程度上只能提供静态类型检查，然后类型的信息就会被擦除，所以像下面这样利用类型参数创建实例的做法编译器不会通过：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;E&gt; <span class="function"><span class="keyword">void</span> <span class="title">append</span><span class="params">(List&lt;E&gt; list)</span> </span>&#123;</span><br><span class="line">    E elem = <span class="keyword">new</span> E();  <span class="comment">// compile-time error</span></span><br><span class="line">    list.add(elem);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但是如果某些场景我们想要需要利用类型参数创建实例，我们应该怎么做呢？可以利用反射解决这个问题：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;E&gt; <span class="function"><span class="keyword">void</span> <span class="title">append</span><span class="params">(List&lt;E&gt; list, Class&lt;E&gt; cls)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    E elem = cls.newInstance();   <span class="comment">// OK</span></span><br><span class="line">    list.add(elem);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以像下面这样调用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; ls = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">append(ls, String.class);</span><br></pre></td></tr></table></figure>
<p>实际上对于上面这个问题，还可以采用Factory和Template两种设计模式解决，感兴趣的朋友不妨去看一下Thinking in Java中第15章中关于Creating instance of types(英文版第664页)的讲解，这里我们就不深入了。</p>
<h3 id="问题四"><a href="#问题四" class="headerlink" title="问题四"></a>问题四</h3><p>我们无法对泛型代码直接使用instanceof关键字，因为Java编译器在生成代码的时候会擦除所有相关泛型的类型信息，正如我们上面验证过的JVM在运行时期无法识别出ArrayList<Integer>和ArrayList<String>的之间的区别：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;E&gt; <span class="function"><span class="keyword">void</span> <span class="title">rtti</span><span class="params">(List&lt;E&gt; list)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (list <span class="keyword">instanceof</span> ArrayList&lt;Integer&gt;) &#123;  <span class="comment">// compile-time error</span></span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">=&gt; &#123; ArrayList&lt;Integer&gt;, ArrayList&lt;String&gt;, LinkedList&lt;Character&gt;, ... &#125;</span><br></pre></td></tr></table></figure>
<p>和上面一样，我们可以使用通配符重新设置bounds来解决这个问题：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">rtti</span><span class="params">(List&lt;?&gt; list)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (list <span class="keyword">instanceof</span> ArrayList&lt;?&gt;) &#123;  <span class="comment">// OK; instanceof requires a reifiable type</span></span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h2><p>接下来我们利用泛型来简单的实现一下工厂模式，首先我们先声明一个接口Factory：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> typeinfo.factory;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Factory</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function">T <span class="title">create</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>接下来我们来创建几个实体类FuelFilter和AirFilter以及FanBelt和GeneratorBelt。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Filter</span> <span class="keyword">extends</span> <span class="title">Part</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FuelFilter</span> <span class="keyword">extends</span> <span class="title">Filter</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Factory</span> <span class="keyword">implements</span> <span class="title">typeinfo</span>.<span class="title">factory</span>.<span class="title">Factory</span>&lt;<span class="title">FuelFilter</span>&gt; </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> FuelFilter <span class="title">create</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> FuelFilter();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AirFilter</span> <span class="keyword">extends</span> <span class="title">Filter</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Factory</span> <span class="keyword">implements</span> <span class="title">typeinfo</span>.<span class="title">factory</span>.<span class="title">Factory</span>&lt;<span class="title">AirFilter</span>&gt; </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> AirFilter <span class="title">create</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> AirFilter();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Belt</span> <span class="keyword">extends</span> <span class="title">Part</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FanBelt</span> <span class="keyword">extends</span> <span class="title">Belt</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Factory</span> <span class="keyword">implements</span> <span class="title">typeinfo</span>.<span class="title">factory</span>.<span class="title">Factory</span>&lt;<span class="title">FanBelt</span>&gt; </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> FanBelt <span class="title">create</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> FanBelt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GeneratorBelt</span> <span class="keyword">extends</span> <span class="title">Belt</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Factory</span> <span class="keyword">implements</span> <span class="title">typeinfo</span>.<span class="title">factory</span>.<span class="title">Factory</span>&lt;<span class="title">GeneratorBelt</span>&gt; </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> GeneratorBelt <span class="title">create</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> GeneratorBelt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Part类的实现如下，注意我们上面的实体类都是Part类的间接子类。在Part类我们注册了我们上面的声明的实体类。所以以后我们如果要创建相关的实体类的话，只需要在调用Part类的相关方法了。这么做的一个好处就是如果的业务中出现了CabinAirFilter或者PowerSteeringBelt的话，我们不需要修改太多的代码，只需要在Part类中将它们注册即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Part</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> List&lt;Factory&lt;? extends Part&gt;&gt; partFactories =</span><br><span class="line">        <span class="keyword">new</span> ArrayList&lt;Factory&lt;? extends Part&gt;&gt;();</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        partFactories.add(<span class="keyword">new</span> FuelFilter.Factory());</span><br><span class="line">        partFactories.add(<span class="keyword">new</span> AirFilter.Factory());</span><br><span class="line">        partFactories.add(<span class="keyword">new</span> FanBelt.Factory());</span><br><span class="line">        partFactories.add(<span class="keyword">new</span> PowerSteeringBelt.Factory());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Random rand = <span class="keyword">new</span> Random(<span class="number">47</span>);</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Part <span class="title">createRandom</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> n = rand.nextInt(partFactories.size());</span><br><span class="line">        <span class="keyword">return</span> partFactories.get(n).create();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> getClass().getSimpleName();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最后我们来测试一下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RegisteredFactories</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            System.out.println(Part.createRandom());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul>
<li><a href="https://docs.oracle.com/javase/tutorial/java/generics/index.html">ORACLE-DOCUMENTATION</a></li>
<li><a href="http://mindview.net/Books/TIJ4">THINKING IN JAVA</a></li>
<li><a href="https://www.safaribooksonline.com/library/view/effective-java-2nd/9780137150021/">EFFECTIVE JAVA</a></li>
</ul>
]]></content>
      <tags>
        <tag>Java</tag>
        <tag>基础知识</tag>
        <tag>泛型</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud笔记：统一配置中心</title>
    <url>/2018/12/04/Spring-Cloud-Config/</url>
    <content><![CDATA[<p>SpringCloud笔记：统一配置中心入门。</p>
<a id="more"></a>


<p>对于配置的重要性，我想我不用进行任何强调，大家都可以明白其重要性。在普通单体应用，我们常使用配置文件(application(*).properties(yml))管理应用的所有配置。这些配置文件在单体应用中非常胜任其角色，并没有让我们感觉到有头疼的地方。但随着微服务框架的引入，微服务数量就会在我们产品中不断增加，之前我们重点考虑的是系统的可伸缩、可扩展性好，但随之就是配置管理的问题就会一一暴露出来。起初微服务器各自管各自的配置，在开发阶段并没什么问题，但到了生产环境管理就会很头疼，如果要大规模更新某项配置，困难就可想而知。<br>为此，在分布式系统中，Spring Cloud提供一个Config子项目，该项目核心就是配置中心，通过一个服务端和多个客户端实现配置服务。我们可使用配置服务器集中的管理所有服务的各种环境配置文件。配置服务中心默认采用Git的方式进行存储，因此我们很容易部署修改，并可以对环境配置进行版本管理。</p>
<h1 id="Spring-Cloud-Config特点"><a href="#Spring-Cloud-Config特点" class="headerlink" title="Spring Cloud Config特点"></a>Spring Cloud Config特点</h1><p>Spring Cloud Config具有中心化、版本控制、支持动态更新和语言独立等特性。其特点是:</p>
<ul>
<li>提供服务端和客户端支持(Spring Cloud Config Server和Spring Cloud Config Client);</li>
<li>集中式管理分布式环境下的应用配置;</li>
<li>基于Spring环境，实现了与Spring应用无缝集成;</li>
<li>可用于任何语言开发的程序;</li>
<li>默认实现基于Git仓库(也支持SVN)，从而可以进行配置的版本管理;</li>
</ul>
<h1 id="Spring-Cloud-Config结构图"><a href="#Spring-Cloud-Config结构图" class="headerlink" title="Spring Cloud Config结构图"></a>Spring Cloud Config结构图</h1><p><img src="/images/SpringCloudConfig-%E7%BB%93%E6%9E%84%E5%9B%BE.png" alt="&quot;Config-结构图&quot;"></p>
<p>从图中可以看出Spring Cloud Config有两个角色(类似Eureka): Server和Client。Spring Cloud Config Server作为配置中心的服务端承担如下作用:</p>
<ul>
<li>拉取配置时更新Git仓库副本，保证是配置为最新;</li>
<li>支持从yml、json、properties等文件加载配置;</li>
<li>配合Eureke可实现服务发现，配合Cloud Bus(这个后面我们在详细说明)可实现配置推送更新;</li>
<li>默认配置存储基于Git仓库(可以切换为SVN)，从而支持配置的版本管理.</li>
<li>而对于，Spring Cloud Config Client则非常方便，只需要在启动配置文件中增加使用Config Server上哪个配置文件即可。</li>
</ul>
<h1 id="示例代码"><a href="#示例代码" class="headerlink" title="示例代码"></a>示例代码</h1><h2 id="构建Config-Server"><a href="#构建Config-Server" class="headerlink" title="构建Config-Server"></a>构建Config-Server</h2><h3 id="编写pom-xml文件"><a href="#编写pom-xml文件" class="headerlink" title="编写pom.xml文件"></a>编写pom.xml文件</h3><p>Config-Server是一个标准的Spring Boot应用，所以pom.xml也是继承之前的Parent：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>twostepsfromjava.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>twostepsfromjava-cloud-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">relativePath</span>&gt;</span>../parent<span class="tag">&lt;/<span class="name">relativePath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>config-server<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">name</span>&gt;</span>Spring Cloud Sample Projects: Config Server<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-config-server<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span>    </span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="编写启动类"><a href="#编写启动类" class="headerlink" title="编写启动类"></a>编写启动类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * TwoStepsFromJava Cloud -- Config Server</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> CD826(CD826Dong@gmail.com)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 1.0.0</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableConfigServer</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Application</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(Application.class, args);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里最重要的就是增加@EnableConfigServer，其它与之前的应用一致。</p>
<h3 id="编写配置文件"><a href="#编写配置文件" class="headerlink" title="编写配置文件"></a>编写配置文件</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="string">server.port=8280</span></span><br><span class="line"></span><br><span class="line"><span class="string">spring.application.name=config-server</span></span><br><span class="line"></span><br><span class="line"><span class="string">spring.cloud.config.server.git.uri=https://github.com/cd826/SpringcloudSamplesConfig</span></span><br><span class="line"><span class="string">spring.cloud.config.server.git.username=your</span> <span class="string">git</span> <span class="string">username</span></span><br><span class="line"><span class="string">spring.cloud.config.server.git.password=your</span> <span class="string">git</span> <span class="string">password</span></span><br></pre></td></tr></table></figure>
<p>这里最重要的是需要配置Git仓库的地址及登录用户名和口令。</p>
<h3 id="创建测试配置文件"><a href="#创建测试配置文件" class="headerlink" title="创建测试配置文件"></a>创建测试配置文件</h3><p>我们在SpringcloudSamplesConfig仓库中增加量个配置文件。</p>
<p>mallWeb.properties，文件内容如下:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="string">foo</span> <span class="string">=</span> <span class="string">bar</span></span><br></pre></td></tr></table></figure>
<p>mallWeb-dev.properties，文件内容如下:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="string">bar</span> <span class="string">=</span> <span class="string">cd826</span></span><br></pre></td></tr></table></figure>
<p>注意：这里编写文件后记得提交，否则会测试失败。</p>
<h3 id="启动测试"><a href="#启动测试" class="headerlink" title="启动测试"></a>启动测试</h3><p>启动config-server. 在终端中我们输入以下命令(或者使用postman):</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl localhost:8280/mallWeb/dev</span><br></pre></td></tr></table></figure>
<p>在终端中会输出以下内容:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">&quot;name&quot;</span>: <span class="string">&quot;mallWeb&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;profiles&quot;</span>: [</span><br><span class="line">        <span class="string">&quot;dev&quot;</span></span><br><span class="line">    ],</span><br><span class="line">    <span class="attr">&quot;label&quot;</span>: <span class="literal">null</span>,</span><br><span class="line">    <span class="attr">&quot;version&quot;</span>: <span class="literal">null</span>,</span><br><span class="line">    <span class="attr">&quot;state&quot;</span>: <span class="literal">null</span>,</span><br><span class="line">    <span class="attr">&quot;propertySources&quot;</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">&quot;name&quot;</span>: <span class="string">&quot;https://github.com/cd826/SpringcloudSamplesConfig/mallWeb-dev.properties&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;source&quot;</span>: &#123;</span><br><span class="line">                <span class="attr">&quot;bar&quot;</span>: <span class="string">&quot;cd826&quot;</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">&quot;name&quot;</span>: <span class="string">&quot;https://github.com/cd826/SpringcloudSamplesConfig/mallWeb.properties&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;source&quot;</span>: &#123;</span><br><span class="line">                <span class="attr">&quot;foo&quot;</span>: <span class="string">&quot;bar&quot;</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里可以看到，我们提交到Git中的配置文件已经能够被config-server正确的读取到。</p>
<h3 id="config-server默认配置"><a href="#config-server默认配置" class="headerlink" title="config-server默认配置"></a>config-server默认配置</h3><p>当我们查看源码会发现在spring-cloud-config-server.jar包中有一个默认配置文件configserver.yml配置文件，也就是说当我们设置spring.application.name=configserver时，就会默认加载该配置文件，该配置文件内容如下:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">info:</span></span><br><span class="line">  <span class="attr">component:</span> <span class="string">Config</span> <span class="string">Server</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">configserver</span></span><br><span class="line">  <span class="attr">jmx:</span></span><br><span class="line">    <span class="attr">default_domain:</span> <span class="string">cloud.config.server</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">config:</span></span><br><span class="line">      <span class="attr">server:</span></span><br><span class="line">        <span class="attr">git:</span></span><br><span class="line">          <span class="attr">uri:</span> <span class="string">https://github.com/spring-cloud-samples/config-repo</span></span><br><span class="line">          <span class="attr">repos:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">patterns:</span> <span class="string">multi-repo-demo-*</span></span><br><span class="line">              <span class="attr">uri:</span> <span class="string">https://github.com/spring-cloud-samples/config-repo</span></span><br><span class="line"></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8888</span></span><br><span class="line"><span class="attr">management:</span></span><br><span class="line">  <span class="attr">context_path:</span> <span class="string">/admin</span></span><br></pre></td></tr></table></figure>
<p>默认使用8888端口，并且从<a href="https://github.com/spring-cloud-samples/config-repo%E8%BF%99%E4%B8%AAGit%E4%BB%93%E5%BA%93%E4%B8%AD%E6%9F%A5%E6%89%BE%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6%E3%80%82">https://github.com/spring-cloud-samples/config-repo这个Git仓库中查找配置文件。</a></p>
<p>因为，在上面的示例中我们重新定义了应用名称和服务端口。</p>
<h2 id="构建config-client"><a href="#构建config-client" class="headerlink" title="构建config-client"></a>构建config-client</h2><p>config-client可以是任何一个基于Spring boot的应用，这里为了讲解方便，我们构建一个非常简单的web工程。</p>
<h3 id="编写pom-xml文件-1"><a href="#编写pom-xml文件-1" class="headerlink" title="编写pom.xml文件"></a>编写pom.xml文件</h3><p>我们的config-client项目需要引入对spring-cloud-starter-config的依赖，如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>twostepsfromjava.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>twostepsfromjava-cloud-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">relativePath</span>&gt;</span>../parent<span class="tag">&lt;/<span class="name">relativePath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>config-client<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">name</span>&gt;</span>Spring Cloud Sample Projects: Config Client<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-config<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    </span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="编写启动类-1"><a href="#编写启动类-1" class="headerlink" title="编写启动类"></a>编写启动类</h3><p>一个标准的Spring Boot启动类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * TwoStepsFromJava Cloud -- Config Client Project</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> CD826(CD826Dong@gmail.com)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 1.0.0</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Application</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(Application.class, args);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="编写测试Controller"><a href="#编写测试Controller" class="headerlink" title="编写测试Controller"></a>编写测试Controller</h3><p>这个测试Controller主要就是验证我们可以从Git仓库中获取配置内容。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Config Client Test Controller</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> CD826(CD826Dong@gmail.com)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 1.0.0</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/cfg&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConfigController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Value(&quot;$&#123;foo&#125;&quot;)</span></span><br><span class="line">    String foo;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value(&quot;$&#123;bar&#125;&quot;)</span></span><br><span class="line">    String bar;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping(value = &quot;/foo&quot;)</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">foo</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> foo + <span class="string">&quot;——&quot;</span> + bar;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="编写配置文件-1"><a href="#编写配置文件-1" class="headerlink" title="编写配置文件"></a>编写配置文件</h3><p>这里编写的配置文件名称为:bootstrap.properties，内容如下:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="string">server.port=8080</span></span><br><span class="line"></span><br><span class="line"><span class="string">spring.application.name=mallWeb</span></span><br><span class="line"><span class="string">spring.cloud.config.profile=dev</span></span><br><span class="line"><span class="string">spring.cloud.config.uri=</span> <span class="string">http://localhost:8280/</span></span><br></pre></td></tr></table></figure>
<p>定义了微服务的名称和profile以及配置服务器的地址。</p>
<p>注意: 这些配置不能够配置在application.properties文件中，因为在Spring Boot启动时有引导上下文和应用上下文的概念，只有将配置服务器信息定义在引导上下文中，才能够从配置服务器中获取到配置信息。否则，服务启动时会报找不到foo变量定义的错误。</p>
<h3 id="启动测试-1"><a href="#启动测试-1" class="headerlink" title="启动测试"></a>启动测试</h3><p>启动后我们可以访问：<a href="http://localhost:8080/cfg/foo%EF%BC%8C%E5%8F%AF%E4%BB%A5%E7%9C%8B%E5%88%B0%E5%A6%82%E4%B8%8B%E7%95%8C%E9%9D%A2%EF%BC%9A">http://localhost:8080/cfg/foo，可以看到如下界面：</a></p>
<p>Config-010<br>说明，我们的config-client已经成功从config-server上获取到配置的数据了。</p>
<h2 id="Spring项目配置加载顺序"><a href="#Spring项目配置加载顺序" class="headerlink" title="Spring项目配置加载顺序"></a>Spring项目配置加载顺序</h2><ul>
<li>这里是列表文本命令行参数</li>
<li>SPRING_APPLICATION_JSON 参数</li>
<li>从java:comp/env 加载 JNDI 属性</li>
<li>Java系统属性 （System.getProperties()）</li>
<li>操作系统环境变量</li>
<li>如果有使用 random.* 属性配置，则使用 RandomValuePropertySource 产生</li>
<li>外部特定应用配置文件 例如：application-{profile}.properties 或者 YAML variants</li>
<li>内部特定应用配置文件 例如：application-{profile}.properties 或者 YAML variants</li>
<li>外部应用配置文件 例如：application.properties 或者 YAML variants</li>
<li>内部应用配置文件 例如：application.properties 或者 YAML variants</li>
<li>加载@Configuration类的 @PropertySource 或者 @ConfigurationProperties 指向的配置文件</li>
<li>默认配置，通过SpringApplication.setDefaultProperties 设置         </li>
</ul>
<h2 id="配置规则详解"><a href="#配置规则详解" class="headerlink" title="配置规则详解"></a>配置规则详解</h2><p>下面我们来看一看Config Client从Config Server中获取配置数据的流程:</p>
<ul>
<li>Config Client启动时，根据bootstrap.properties中配置的应用名称(application)、环境名(profile)和分支名(label)，向Config Server请求获取配置数据;</li>
<li>Config Server根据Config Client的请求及配置，从Git仓库(这里以Git为例)中查找符合的配置文件;</li>
<li>Config Server将匹配到的Git仓库拉取到本地，并建立本地缓存;</li>
<li>Config Server创建Spring的ApplicationContext实例，并根据拉取的配置文件，填充配置信息，然后将该配置信息返回给Config Client;</li>
<li>Config Client获取到Config Server返回的配置数据后，将这些配置数据加载到自己的上下文中。同时，因为这些配置数据的优先级高于本地Jar包中的配置，因此将不再加载本地的配置。</li>
</ul>
<p>那么，Config Server又是如何与Git仓库中的配置文件进行匹配的呢？通常，我们会为一个项目建立类似如下的配置文件:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mallweb.properties: 基础配置文件;</span><br><span class="line">mallweb-dev.properties: 开发使用的配置文件;</span><br><span class="line">mallweb-test.properties: 测试使用的配置文件;</span><br><span class="line">mallweb-prod.properties: 生产环境使用的配置文件;</span><br></pre></td></tr></table></figure>
<p>当我们访问Config Server的端点时，就会按照如下映射关系来匹配相应的配置文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#123;application&#125;&#x2F;&#123;profile&#125;[&#x2F;&#123;label&#125;]</span><br><span class="line">&#x2F;&#123;application&#125;-&#123;profile&#125;.yml</span><br><span class="line">&#x2F;&#123;label&#125;&#x2F;&#123;application&#125;-&#123;profile&#125;.yml</span><br><span class="line">&#x2F;&#123;application&#125;-&#123;profile&#125;.properties</span><br><span class="line">&#x2F;&#123;label&#125;&#x2F;&#123;application&#125;-&#123;profile&#125;.properties</span><br><span class="line">上面的Url将会映射为格式为:&#123;application&#125;-&#123;profile&#125;.properties(yml)的配置文件。另外，label则对应Git上分支名称，是一个可选参数，如果没有则为默认的master分支。</span><br></pre></td></tr></table></figure>

<p>而Config-Client的bootstrap.properties配置对应如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">spring.application.name &lt;&#x3D;&#x3D;&gt; application;</span><br><span class="line">spring.cloud.config.profile &lt;&#x3D;&#x3D;&gt; profile;</span><br><span class="line">spring.cloud.config.label &lt;&#x3D;&#x3D;&gt; label.</span><br></pre></td></tr></table></figure>
<p>2.1 Git仓库配置<br>Config Server默认使用的就是Git，所以配置也非常简单，如上面的配置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(application.properties):</span><br><span class="line"></span><br><span class="line">spring.cloud.config.server.git.uri&#x3D;http:&#x2F;&#x2F;</span><br><span class="line">spring.cloud.config.server.git.username&#x3D;username</span><br><span class="line">spring.cloud.config.server.git.password&#x3D;password</span><br></pre></td></tr></table></figure>
<p>那么客户端在请求时服务端就会到该仓库中进行查找。</p>
<p>2.1.1 使用占位符<br>在服务端配置中我们也可以使用{application}、{profile} 和 {label}占位符，如下:</p>
<p>spring.cloud.config.server.git.uri=<a href="http://github.com/cd826/%7Bapplication%7D">http://github.com/cd826/{application}</a><br>spring.cloud.config.server.git.username=username<br>spring.cloud.config.server.git.password=password<br>这样，我们就可以为每一个应用客户端创建一个单独的仓库。</p>
<p>这里需要注意的是，如果Git的分支或标签中包含”/“时，在{label}参数中需要使用”(_)”替代，这个主要是避免与Http URL转义符处理的冲突。</p>
<p>2.1.2 使用模式匹配<br>我们也可以使用{application}/{profile}进行模式匹配，以便获取到相应的配置文件。配置示例如下:</p>
<p>spring.cloud.config.server.git.uri=<a href="https://github.com/spring-cloud-samples/config-repo">https://github.com/spring-cloud-samples/config-repo</a></p>
<p>spring.cloud.config.server.git.repos.simple=<a href="https://github.com/simple/config-repo">https://github.com/simple/config-repo</a></p>
<p>spring.cloud.config.server.git.repos.special.pattern=special*/dev*,<em>special</em>/dev*<br>spring.cloud.config.server.git.repos.special.uri=<a href="https://github.com/special/config-repo">https://github.com/special/config-repo</a></p>
<p>spring.cloud.config.server.git.repos.local.pattern=local*<br>spring.cloud.config.server.git.repos.local.uri=file:/home/configsvc/config-repo<br>如果模式中需要配置多个值，那么可以使用逗号分隔。</p>
<p>如果{application}/{profile}没有匹配到任何资源，则使用spring.cloud.config.server.git.uri配置的默认URI。</p>
<p>当我们使用yml类型的文件进行配置时，如果模式属性是一个YAML数组，也可以使用YAML数组格式来定义。这样可以设置成多个配个配置文件，如:</p>
<p>spring:<br>  cloud:<br>    config:<br>      server:<br>        git:<br>          uri: <a href="https://github.com/spring-cloud-samples/config-repo">https://github.com/spring-cloud-samples/config-repo</a><br>          repos:<br>            development:<br>              pattern:<br>                - */development<br>                - */staging<br>              uri: <a href="https://github.com/development/config-repo">https://github.com/development/config-repo</a><br>            staging:<br>              pattern:<br>                - */qa<br>                - */production<br>              uri: <a href="https://github.com/staging/config-repo">https://github.com/staging/config-repo</a><br>2.1.3 搜索目录<br>当我们把配置文件存放在Git仓库中子目录中时，可以通过设置serch-path来指定该目录。同样，serch-path也支持上面的占位符。示例如下:</p>
<p>spring.cloud.config.server.git.uri=<a href="https://github.com/spring-cloud-samples/config-repo">https://github.com/spring-cloud-samples/config-repo</a><br>spring.cloud.config.server.git.searchPaths=foo,bar*<br>这样系统就会自动搜索foo的子目录，以及以bar开头的文件夹中的子目录。</p>
<p>2.1.4 SSH配置<br>如果你不想使用HTTPS和用户认证，也可以直接使用SSH，这时候我们只需要将ssh需要的keys存储在～/.ssh目录即可，并将所配置的uri指向SSH地址即可，如：<a href="mailto:&#x67;&#105;&#116;&#x40;&#x67;&#105;&#116;&#x68;&#117;&#98;&#x2e;&#x63;&#111;&#x6d;">&#x67;&#105;&#116;&#x40;&#x67;&#105;&#116;&#x68;&#117;&#98;&#x2e;&#x63;&#111;&#x6d;</a>:cd826/SpringcloudSamplesConfig。</p>
<p>如果你清楚的知道你的~/.git目录，那么你可以使用git config –global来配置。否则可以使用全局配置，比如：git config –global http.sslVerify false。</p>
<p>2.1.4 代理<br>Config-Server会使用JGit访问配置库，因此我们可以在~/.git/config下配置HTTPS所使用的代理，也可以使用JVM系统属性-Dhttps.proxyHost和-Dhttps.proxyPort来配置。</p>
<p>2.1.5 本地缓存<br>当Config-Server从Git(或SVN)中获取了配置信息后，将会在本地的文件系统中存储一份。默认将存储在系统临时目录下，并且以config-repo-作为开头，在Linux系统中默认存储的目录为/tmp/config-repo-<randomid>。Config-Server将配置信息存储在本地可以有效的防止当Git仓库出现故障而无法访问的问题，当Config-Server无法访问到Git仓库时就会读取之前存储在本地文件中的配置，然后将这些配置信息返回给Config-Client。比如，当我们断开网络进行测试，当我们启动Config-Server时会在控制台中看到以下输出：</p>
<p>17:18:01 285 [http-nio-8280-exec-1] WARN  o.s.c.c.s.e.MultipleJGitEnvironmentRepository - Could not fetch remote for master remote: <a href="https://github.com/cd826/SpringcloudSamplesConfig">https://github.com/cd826/SpringcloudSamplesConfig</a><br>17:18:01 660 [http-nio-8280-exec-1] INFO  o.s.c.a.AnnotationConfigApplicationContext - Refreshing<br>17:18:01 710 [http-nio-8280-exec-1] INFO  o.s.b.f.a.AutowiredAnnotationBeanPostProcessor - JSR-330 ‘javax.inject.Inject’ annotation found and supported for autowiring<br>17:18:01 782 [http-nio-8280-exec-1] INFO  o.s.c.c.s.e.NativeEnvironmentRepository - Adding property source: file:/Users/cd826/MyWork/springcloud-sample-projects/config/config-server/tmp/mallWeb-dev.properties<br>17:18:01 782 [http-nio-8280-exec-1] INFO  o.s.c.c.s.e.NativeEnvironmentRepository - Adding property source: file:/Users/cd826/MyWork/springcloud-sample-projects/config/config-server/tmp/mallWeb.properties<br>Spring Cloud 官方文档建议我们在Config-Server中指定本地文件路径，以避免出现不可预知的错误。可以使用下面的属性配置来指定本地文件路径:</p>
<h2 id="Git仓库"><a href="#Git仓库" class="headerlink" title="Git仓库"></a>Git仓库</h2><p>spring.cloud.config.server.git.basedir=tmp/</p>
<h2 id="SVN仓库"><a href="#SVN仓库" class="headerlink" title="SVN仓库"></a>SVN仓库</h2><p>spring.cloud.config.server.svn.basedir=tmp/<br>2.2 SVN配置<br>如果你的项目中使用的是SVN而不是Git，那么只需要在Config-Server中进行如下修改就可以支持SVN仓库。</p>
<p>2.2.1 修改pom.xml<br>在pom文件中增加以下依赖:</p>
<dependency>
    <groupId>org.tmatesoft.svnkit</groupId>
    <artifactId>svnkit</artifactId>
</dependency>
2.2.2 修改appliaction.properties
spring.cloud.config.server.svn.uri={your svn server}
spring.cloud.config.server.svn.username=username
spring.cloud.config.server.svn.password=password
2.3 文件系统
如果你的Config-Server中不想使用Git或SVN，那么我们也可以直接从当前classpath或文件系统中加载相应的配置文件，只需在配置文件中设置如下：

<p>spring.profiles.active=native<br>注意Config-Server默认会从classpath下加载，我们可以使用spring.cloud.config.server.native.searchLocations属性来设置配置文件的目录。对于文件路径，我们的配置必须以file:开头，如果是Windows系统对于绝对路径我们还得对/进行转义，比如，在Windows下我们需要配置如下：file:///${user.home}/config-repo。</p>
<p>此外，当我们使用文件系统作为配置文件仓库时，spring.cloud.config.server.native.searchLocations的配置也是支持{application} 、{profile} 和 {label} 占位符的。</p>
<p>Spring Cloud 官方还是推荐在测试和开发的时候可以使用文件系统，但是在正式环境中尽量还是使用Git或者SVN。</p>
<p>另，Spring Cloud Config中还支持另外一种配置文件方式：Vault Server，这个后面有空我们再探讨。</p>
<ol start="3">
<li>安全保护</li>
<li>1 Config-Server访问安全<br>对于我们存储在配置中心的一些配置内容，总会有一些是敏感信息，比如数据库连接的用户名和密码，你总不能直接裸奔吧，所以我们还是需要对Config-Server做一些安全控制。当然，对于Config-Server的安全控制有很多种，比如：物理网络限制、OAuth2授权等。但是，在这里因为我们使用的是SpringBoot，所以使用SpringSecurity会更容易也更简单。这时候，我们只需要在Config-Server中增加如下依赖:</li>
</ol>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
此时，当我们启动Config-Server时，SpringSecurity会默认为我们生产一个访问密码，这种方式常常不是我们需要的，所以一般我们还需要在配置文件中配置用户名和密码，比如：

<p>security.user.name=cd826<br>security.user.password=pwd<br>这样，当我们需要访问Config-Server时就会弹出用户认证对话框。此时，对于Config-Client我们需要在配置文件中增加用户和访问口令的配置，如下：</p>
<p>spring.cloud.config.username=cd826<br>spring.cloud.config.password=pwd<br>3.2 加密与解密<br>访问安全是对整体的控制，多数情况下我们还需要对敏感内容加密后存储，比如之前所说的数据库访问的用户名称和登录口令。很幸运，Spring Cloud Config为我们提供相应的支持。</p>
<p>Spring Cloud Config提供了两种加解密方式: 1)对称加密; 2)非对称加密。在描述如何使用之前，我们先看看一些使用前提。</p>
<p>3.2.1 安装JCE(Java Cryptography Extension)<br>Spring Cloud Config所提供的加解密依赖JCE，因为，JDK中没有默认提供，所以我们需要先安装JCE。安装方法也比较简单，就是下载相应的Jar包，然后把这些包替换$JDK_HOME/jar/lib/security相应的文件，对于JDK8下载地址为:JCE for JDK8.</p>
<p>3.2.2 加解密端点<br>另外，Spring Cloud Config还提供了两个端点进行加密和解密，如下：</p>
<p>/encrypt: 加密端点，使用格式如下: curl $CONFIG_SERVER/encrypt -d 所要加密的内容<br>/decrypt: 解密端点，使用格式如下: curl $CONFIG_SERVER/decrypt -d 所要解密的内容<br>注意：当你测试中所加解密中包含特殊字符时，需要进行URL编码，这时候你需要使用–data-urlencode而不是-d.</p>
<p>3.2.3 对称加密<br>对称加解密的配置非常简单。我们只需要在配置文件中增加加解密所使用的密钥即可，如:</p>
<p>encrypt.key=cd826_key<br>配置好之后，你可以启动Config-Server，并使用上面所说的端点进行加解密测试。</p>
<p>对于，配置文件我们需要为加密的内容增加一个{cipher}前导符。如：</p>
<p>spring.datasource.username=dbuser<br>spring.datasource.password={cipher}FKSAJDFGYOS8F7GLHAKERGFHLSAJ<br>但是，如果你使用的配置文件是yml格式，那么需要使用单引号把加密内容引起来，如下：</p>
<p>spring:<br>    datasource:<br>        username:dbuser<br>        password:’{cipher}FKSAJDFGYOS8F7GLHAKERGFHLSAJ’<br>3.2.4 非对称加密<br>非对称加密相对于对称加密来说复杂了一些，首先我们需要借助Java的keytool生成密钥对，然后创建Key Store并复制到服务器目录下。对于keytool的使用可以参考这里:《Spring Cloud入门教程(番外篇四): Keytool证书工具》。</p>
<p>使用keytool生成Key Store，命令如下:<br>$ keytool -genkeypair -alias tsfjckey -keyalg RSA <br>  -dname “CN=Mall Web,OU=TwoStepsFromJava,O=Organization,L=city,S=province,C=china” \<br>  -keypass javatwostepsfrom -keystore server.jks -storepass twostepsfromjava<br>将所生成server.jks拷贝到项目的resources目录下(Config-Server)。</p>
<p>修改配置文件:</p>
<p>encrypt.key-store.location=server.jks<br>encrypt.key-store.alias=tsfjckey<br>encrypt.key-store.password=twostepsfromjava<br>encrypt.key-store.secret=javatwostepsfrom<br>非对称加密相对于对称加密来说配置也复杂，但安全性也会高很多。</p>
<p>3.2.5 使用多个加密Key<br>也许，我们需要对不同的敏感信息使用不同的加密key，这时候我们的配置文件只需要按如下进行编写:</p>
<p>foo.bar={cipher}{key:testkey}…<br>Config-Server在解密的时候就会尝试从配置文件中获取testkey的做为密钥。</p>
<ol start="4">
<li>高可用配置</li>
<li>1 整合Eureka<br>看到这里，可能有些童鞋已经发现，我们在Config-Client中配置config.uri时使用的具体的地址，那么是否可以使用之前的Eureka呢？答案是肯定，我们可以把Config-Server和其它微服务一样作为一个服务基本单元。我们只需要进行如下修改即可。</li>
</ol>
<p>4.1.1 Config-Server改造<br>在pom.xml中增加如下依赖:</p>
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-eureka</artifactId>
</dependency>
在配置文件中配置我们服务的名称，及之前我们所编写Eureka服务器的地址:

<p>spring.application.name=config-server</p>
<p>eureka.client.service-url.defaultZone=<a href="http://localhost:8260/eureka">http://localhost:8260/eureka</a><br>启动类:</p>
<p>/**</p>
<ul>
<li><p>TwoStepsFromJava Cloud – Config Server</p>
</li>
<li></li>
<li><p>@author CD826(<a href="mailto:&#x43;&#x44;&#56;&#x32;&#54;&#68;&#x6f;&#110;&#x67;&#64;&#x67;&#109;&#x61;&#x69;&#x6c;&#46;&#x63;&#x6f;&#x6d;">&#x43;&#x44;&#56;&#x32;&#54;&#68;&#x6f;&#110;&#x67;&#64;&#x67;&#109;&#x61;&#x69;&#x6c;&#46;&#x63;&#x6f;&#x6d;</a>)</p>
</li>
<li><p>@since 1.0.0</p>
</li>
<li><p>/<br>@SpringBootApplication<br>@EnableConfigServer<br>@EnableDiscoveryClient<br>public class Application {</p>
<p>  public static void main(String[] args) {</p>
<pre><code>  SpringApplication.run(Application.class, args);</code></pre>
<p>  }</p>
</li>
</ul>
<p>}<br>增加@EnableDiscoveryClient注解。</p>
<p>Ok，到这里为止，Config-Server修改已经完成。如果我们启动Config-Server就会在Eureka服务器上看到相应的服务注册。</p>
<p>4.1.2 Config-Client改造<br>在pom.xml中增加如下依赖:</p>
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-eureka</artifactId>
</dependency>
配置文件修改，注意这里的配置文件为:bootstrap.properties:

<p>spring.application.name=mallWeb<br>spring.cloud.config.profile=dev</p>
<p>eureka.client.service-url.defaultZone=<a href="http://localhost:8260/eureka">http://localhost:8260/eureka</a></p>
<p>spring.cloud.config.discovery.enabled=true<br>spring.cloud.config.discovery.service-id=config-server<br>修改启动类:</p>
<p>/**</p>
<ul>
<li><p>TwoStepsFromJava Cloud – Config Client Project</p>
</li>
<li></li>
<li><p>@author CD826(<a href="mailto:&#x43;&#x44;&#56;&#x32;&#x36;&#x44;&#x6f;&#110;&#x67;&#x40;&#103;&#x6d;&#x61;&#105;&#108;&#46;&#99;&#x6f;&#109;">&#x43;&#x44;&#56;&#x32;&#x36;&#x44;&#x6f;&#110;&#x67;&#x40;&#103;&#x6d;&#x61;&#105;&#108;&#46;&#99;&#x6f;&#109;</a>)</p>
</li>
<li><p>@since 1.0.0</p>
</li>
<li><p>/<br>@EnableDiscoveryClient<br>@SpringBootApplication<br>public class Application {</p>
<p>  public static void main(String[] args) {</p>
<pre><code>  SpringApplication.run(Application.class, args);</code></pre>
<p>  }</p>
</li>
</ul>
<p>}</p>
<p>这样就完成了从Eureka中获取Config-Server的服务信息。</p>
<p>这里最重要的就是在配置中增加: spring.cloud.config.discovery.enabled=true，并将原来所配置的spring.cloud.config.uri，修改为spring.cloud.config.discovery.service-id。</p>
<p>4.2 快速失败与响应<br>4.2.1 开启Config-Server启动加载<br>默认情况下，只有当客户端请求时服务端才会从配置的Git仓库中进行加载，我们可以通过设置clone-on-start，让服务端在启动时就会加载。</p>
<p>spring.cloud.config.server.git.uri=<a href="https://git/common/config-repo.git">https://git/common/config-repo.git</a></p>
<p>spring.cloud.config.server.git.repos.team-a.pattern=team-a-*<br>spring.cloud.config.server.git.repos.team-a.clone-on-start=true<br>spring.cloud.config.server.git.repos.team-a.uri=<a href="http://git/team-a/config-repo.git">http://git/team-a/config-repo.git</a></p>
<p>spring.cloud.config.server.git.repos.team-b.pattern=team-b-*<br>spring.cloud.config.server.git.repos.team-b.clone-on-start=false<br>spring.cloud.config.server.git.repos.team-b.uri=<a href="http://git/team-b/config-repo.git">http://git/team-b/config-repo.git</a></p>
<p>spring.cloud.config.server.git.repos.team-c.pattern=team-c-*<br>spring.cloud.config.server.git.repos.team-c.uri=<a href="http://git/team-a/config-repo.git">http://git/team-a/config-repo.git</a><br>上面的配置，对于team-a的则在Config-Server启动时就会加载相应的配置，而对于其它则不会。当然，我们可以通过设置spring.cloud.config.server.git.clone-on-start的值来进行全局配置。</p>
<p>4.2.2 开启Config-Client快速失败<br>在一些情况下，我们希望启动一个服务时无法连接到服务端能够快速返回失败，那么可以通过下面的配置来设置:</p>
<p>spring.cloud.config.fail-fast=true<br>4.2.3 设置Config-Client重试<br>如果在启动时Config-Server碰巧不可以使用，你还想后面再进行重试，那么我们开始开启Config-Client的重试机制。首先，我们需要配置：</p>
<p>spring.cloud.config.fail-fast=true<br>然后，我们需要在我们的的依赖中增加：</p>
<dependency>
    <groupId>org.springframework.retry</groupId>
    <artifactId>spring-retry</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId>
</dependency>
这样，我们就可以为Config-Client开启了重试机制，当启动连接Config-Server失败时，Config-Client会继续尝试连接Config-Server，默认会尝试连接6次，时间间隔初始为1000毫秒，后面每次尝试连接会按照1.1倍数增加尝试连接时间的间隔，如果最后还不能够连接到Config-Server才会返回错误。我们可以通过在配置文件中复写spring.cloud.config.retry.*来进行相关配置。

<p>如果你想全权控制重试机制，可以实现一个类型为:RetryOperationsInterceptor的类，并把bean的id设置为:configServerRetryInterceptor。</p>
<p>4.3 动态刷新配置<br>Config-Client中提供了一个refresh端点来实现配置文件的刷新。要想使用该功能，我们需要在Config-Client的pom.xml文件中增加以下依赖:</p>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
这样，当修改配置文件并提交到Git仓库后，就可以使用:http://localhost:8080/refresh刷新本地的配置数据。

<p>但是，最好的方式还是和Spring Cloud Bus进行整合，这样才能实现配置的自动分发，而不是需要手工去刷新配置。</p>
]]></content>
      <tags>
        <tag>Java</tag>
        <tag>SpringCloud</tag>
        <tag>配置中心</tag>
      </tags>
  </entry>
  <entry>
    <title>[转载]左耳朵耗子：什么是函数式编程</title>
    <url>/2018/05/15/%E5%B7%A6%E8%80%B3%E6%9C%B5%E8%80%97%E5%AD%90%EF%BC%9A%E4%BB%80%E4%B9%88%E6%98%AF%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B%EF%BC%9F/</url>
    <content><![CDATA[<p>本文结合递归、map 和 reduce，以及 pipeline 等技术，对比了非函数式编程和函数式编程在解决相同问题时的不同处理思路，让你对函数式编程范式有了清晰明确的认知。并在文末引入了 decorator(修饰器)，使得将普通函数管道化成为一件轻而易举的事情。</p>
<a id="more"></a>

<p>我们知道，虽然C语言简单灵活，能够让程序员在高级语言特性之上轻松进行底层上的微观控制，被誉为“高级语言中的汇编语言”，但其基于过程和底层的设计初衷又成了它的短板。</p>
<p>在程序世界中，编程工作更多的是解决业务上的问题，而不是计算机的问题，我们需要更为贴近业务更为抽象的语言，如面向对象语言C++和Java等。</p>
<p>C++很大程度上解决了C语言中的各种问题和不便，尤其是通过类、模板、虚函数和运行时识别等解决了C语言的泛型编程问题。然而，如何做更为抽象的泛型呢？答案就是函数式编程(Functional Programming)。</p>
<h2 id="函数式编程"><a href="#函数式编程" class="headerlink" title="函数式编程"></a>函数式编程</h2><p>相对于计算机的历史而言，函数式编程其实是一个非常古老的概念。函数式编程的基础模型来源于λ演算，而λ演算并非设计于在计算机上执行。它是由Alonzo Church和Stephen Cole Kleene在20世纪30年代引入的一套用于研究函数定义、函数应用和递归的形式系统。<br>如Alonzo所说，像booleans、integers或者其他的数据结构都可以被函数取代掉。<br>我们来看一下函数式编程，它的理念就是借用于数学中的代数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f(x)=<span class="number">5</span>x^<span class="number">2</span>+<span class="number">4</span>x+<span class="number">3</span></span><br><span class="line">g(x)=<span class="number">2</span>f(x)+<span class="number">5</span>=<span class="number">10</span>x^<span class="number">2</span>+<span class="number">8</span>x+<span class="number">11</span></span><br><span class="line">h(x)=f(x)+g(x)=<span class="number">15</span>x^<span class="number">2</span>+<span class="number">12</span>x+<span class="number">14</span></span><br></pre></td></tr></table></figure>
<p>假设 f(x) 是一个函数，g(x) 是第二个函数，把 f(x) 这个函数套下来，并展开。然后还可以定义一个由两个一元函数组合成的二元函数。还可以做递归，下面这个函数定义就是斐波那契数列。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f(x)=f(x<span class="number">-1</span>)+f(x<span class="number">-2</span>)</span><br></pre></td></tr></table></figure>
<p>对于函数式编程来说，其只关心，定义输入数据和输出数据相关的关系，数学表达式里面其实是在做一种映射(mapping)，输入的数据和输出的数据关系是什么样的，是用函数来定义的。</p>
<h3 id="函数式编程的特点"><a href="#函数式编程的特点" class="headerlink" title="函数式编程的特点"></a>函数式编程的特点</h3><h4 id="特征："><a href="#特征：" class="headerlink" title="特征："></a>特征：</h4><ul>
<li>stateless：函数不维护任何状态。函数式编程的核心精神是 stateless，简而言之就是它不能存在状态，你给我数据我处理完扔出来，里面的数据是不变的。</li>
<li>immutable：输入数据是不能动的，动了输入数据就有危险，所以要返回新的数据集。</li>
</ul>
<h4 id="优势："><a href="#优势：" class="headerlink" title="优势："></a>优势：</h4><ul>
<li>没有状态就没有伤害。</li>
<li>并行执行无伤害。</li>
<li>Copy-Paste 重构代码无伤害。</li>
<li>函数的执行没有顺序上的问题。</li>
</ul>
<h4 id="函数式编程额外带来的好处："><a href="#函数式编程额外带来的好处：" class="headerlink" title="函数式编程额外带来的好处："></a>函数式编程额外带来的好处：</h4><ul>
<li>惰性求值<br>这需要编译器的支持。表达式不在它被绑定到变量之后就立即求值，而是在该值被取用的时候求值。也就是说，语句如x:=expression;(把一个表达式的结果赋值给一个变量)显式地调用这个表达式被计算并把结果放置到x中，但是先不管实际在x中的是什么，直到通过后面的表达式中到x的引用而有了对它的值的需求的时候，而后面表达式自身的求值也可以被延迟，最终为了生成让外界看到的某个符号而计算这个快速增长的依赖树。</li>
<li>确定性<br>所谓确定性，就是像在数学中那样，f(x)=y这个函数无论在什么场景下，都会得到同样的结果，这个我们称之为函数的确定性。而不是像程序中的很多函数那样，同一个参数，却会在不同的场景下计算出不同的结果。所谓不同的场景，就是我们的函数会根据运行中的状态信息的不同而发生变化。       </li>
</ul>
<p>我们知道，因为状态，在并行执行和copy-paste时引发bug的概率是非常高的，所以没有状态就没有伤害，就像没有依赖就没有伤害一样，并行执行无伤害，copy代码无伤害，因为没有状态，代码怎样拷都行。</p>
<h4 id="劣势："><a href="#劣势：" class="headerlink" title="劣势："></a>劣势：</h4><ul>
<li>数据复制比较严重<br>有一些人可能会觉得这会对性能造成影响。其实，这个劣势并不见得会导致性能不好。因为没有状态，所以代码在并行上根本不需要锁(不需要对状态修改的锁)，所以可以拼命地并发，反而可以让性能很不错。比如：Erlang就是其中的代表。</li>
</ul>
<h4 id="各语言对函数式编程的支持程度："><a href="#各语言对函数式编程的支持程度：" class="headerlink" title="各语言对函数式编程的支持程度："></a>各语言对函数式编程的支持程度：</h4><p>对于纯函数式(也就是完全没有状态的函数)的编程来说，各个语言支持的程度如下：</p>
<ul>
<li>完全纯函数式：Haskell</li>
<li>容易写纯函数：F#,Ocaml,Clojure,Scala</li>
<li>纯函数需要花点精力：C#,Java,Java</li>
</ul>
<p>完全纯函数的语言，很容易写成函数，纯函数需要花精力。只要所谓的纯函数的问题，传进来的数据不改，改完的东西复制一份拷出去，然后没有状态。<br>但是很多人并不习惯函数式编程，因为函数式编程和过程式编程的思维方式完全不一样。过程式编程是在把具体的流程描述出来，所以可以不假思索，而函数式编程的抽象度更大，在实现方式上，函数套函数，函数返回函数，函数里定义函数……把人搞得很糊涂。</p>
<h3 id="函数式编程用到的技术"><a href="#函数式编程用到的技术" class="headerlink" title="函数式编程用到的技术"></a>函数式编程用到的技术</h3><p>下面是函数式编程用到的一些技术：</p>
<ul>
<li><p><strong>first class function(头等函数)</strong><br>这个技术可以让你的函数就像变量一样来使用。也就是说，你的函数可以像变量一样被创建、修改，并当成变量一样传递、返回，或是在函数中嵌套函数。</p>
</li>
<li><p><strong>tail recursion optimization(尾递归优化)</strong><br>我们知道递归的害处，那就是如果递归很深的话，stack受不了，并会导致性能大幅度下降。因此，我们使用尾递归优化技术——每次递归时都会重用stack，这样能够提升性能。当然，这需要语言或编译器的支持。Python就不支持。</p>
</li>
<li><p><strong>map&amp;reduce</strong><br>这个技术不用多说了，函数式编程最常见的技术就是对一个集合做Map和Reduce操作。这比起过程式的语言来说，在代码上要更容易阅读。(传统过程式的语言需要使用for/while循环，然后在各种变量中把数据倒过来倒过去的)这个很像C++STL中foreach、find_if、count_if等函数的玩法。</p>
</li>
<li><p><strong>pipeline(管道)</strong><br>这个技术的意思是，将函数实例成一个一个的action，然后将一组action放到一个数组或是列表中，再把数据传给这个action list，数据就像一个pipeline一样顺序地被各个函数所操作，最终得到我们想要的结果。</p>
</li>
<li><p><strong>recursing(递归)</strong><br>递归最大的好处就简化代码，它可以把一个复杂的问题用很简单的代码描述出来。注意：递归的精髓是描述问题，而这正是函数式编程的精髓。</p>
</li>
<li><p><strong>currying(柯里化)</strong><br>将一个函数的多个参数分解成多个函数，然后将函数多层封装起来，每层函数都返回一个函数去接收下一个参数，这可以简化函数的多个参数。在C++中，这很像STL中的bind1st或是bind2nd。</p>
</li>
<li><p><strong>higher order function(高阶函数)</strong><br>所谓高阶函数就是函数当参数，把传入的函数做一个封装，然后返回这个封装函数。现象上就是函数传进传出，就像面向对象对象满天飞一样。这个技术用来做Decorator很不错。</p>
</li>
</ul>
<h3 id="函数式编程举例"><a href="#函数式编程举例" class="headerlink" title="函数式编程举例"></a>函数式编程举例</h3><p>上面这些技术太抽象了，我们还是从一个最简单的例子开始。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">//非函数式，不是pure funciton，有状态</span><br><span class="line">int cnt;</span><br><span class="line">void increment()</span><br><span class="line">&#123;</span><br><span class="line">    cnt++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里有个全局变量，调这个全局函数变量++，这里面是有状态的，这个状态在外部。所以，如果是多线程的话，这里面的代码是不安全的。<br>如果写成纯函数，应该是下面这个样子。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">//函数式，pure function，无状态</span><br><span class="line">int increment(int cnt)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> cnt+<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个是你传给我什么，我就返回这个值的+1值，你会发现，代码随便拷，而且与线程无关，代码在并行时候不用锁，因为是复制了原有的数据，并返回了新的数据。<br>我们再来看另一个例子：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">inc</span>(<span class="params">x</span>):</span> </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">incx</span>(<span class="params">y</span>):</span> </span><br><span class="line">        <span class="keyword">return</span> x+y </span><br><span class="line">    <span class="keyword">return</span> incx</span><br><span class="line">inc2 = inc(<span class="number">2</span>)</span><br><span class="line">inc5 = inc(<span class="number">5</span>)</span><br><span class="line"><span class="keyword">print</span> inc2(<span class="number">5</span>)               <span class="comment"># 输出 7</span></span><br><span class="line"><span class="keyword">print</span> inc5(<span class="number">5</span>)               <span class="comment"># 输出 10</span></span><br></pre></td></tr></table></figure>
<p>上面这段 Python 的代码，开始有点复杂了。我们可以看到上面那个例子inc()函数返回了另一个函数incx()，于是可以用inc()函数来构造各种版本的inc函数，比如：inc2()和inc5()。这个技术其实就是上面所说的currying技术。从这个技术上，你可能体会到函数式编程的理念。<br>把函数当成变量来用，关注描述问题而不是怎么实现，这样可以让代码更易读。因为函数返回里面的这个函数，所以函数关注的是表达式，关注的是描述这个问题，而不是怎么实现这个事情。   </p>
<h2 id="Lisp-语言介绍"><a href="#Lisp-语言介绍" class="headerlink" title="Lisp 语言介绍"></a>Lisp 语言介绍</h2><p>要说函数式语言，不可避免地要说一下 Lisp。<br>下面，我们再来看看Scheme语言(Lisp 的一个方言)的函数式玩法。在Scheme里，所有的操作都是函数，包括加减乘除这样的东西。所以，一个表达式是这样的形式——(函数名 参数1 参数2)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">(define (plus x y)  (+ x y))            <span class="comment"># 用内置的 + 函数定义了一个新 plus函数</span></span><br><span class="line">(define (times x y) (* x y))            <span class="comment"># 用内置的 * 函数定义了一个新的times函数</span></span><br><span class="line">(define (square x)  (times x x))        <span class="comment"># 用之前的 times 函数定义了一个square函数</span></span><br></pre></td></tr></table></figure>
<p>下面这个函数定义了f(x)=5*x^2+10</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">;;; f(x)=<span class="number">5</span>*x^<span class="number">2</span>+<span class="number">10</span></span><br><span class="line">( define </span><br><span class="line">    (f x) </span><br><span class="line">        (plus <span class="number">10</span> (times <span class="number">5</span> (square x))) </span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>也可以这样定义——使用lambda匿名函数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">(define f </span><br><span class="line">    (<span class="keyword">lambda</span> (x) </span><br><span class="line">        (define plus </span><br><span class="line">            (<span class="keyword">lambda</span> (a b) (+ a b))) </span><br><span class="line">        (define times </span><br><span class="line">            (<span class="keyword">lambda</span> (a b) (* a b))) </span><br><span class="line">        (plus <span class="number">10</span> (times <span class="number">5</span> (times x x)))</span><br><span class="line">    )</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>在上面的这个代码里，我们使用lambda来定义函数f，然后也同样用lambda定义了两个函数：plus和times。最后，由(plus 10 (times 5 (times x x)))定义了f。<br>我们再来看一个阶乘的示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">;;; recursion</span><br><span class="line">(define factoral (<span class="keyword">lambda</span> (x) </span><br><span class="line">                    (<span class="keyword">if</span> (&lt;= x <span class="number">1</span>) <span class="number">1</span> </span><br><span class="line">                        (* x (factoral (- x <span class="number">1</span>))))))</span><br><span class="line">(newline)</span><br><span class="line">(display(factoral <span class="number">6</span>))</span><br></pre></td></tr></table></figure>
<p>下面是另一个版本的，使用了尾递归。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">;;; another version of recursion</span><br><span class="line">(define (factoral_x n) </span><br><span class="line">    (define (iter product counter) </span><br><span class="line">        (<span class="keyword">if</span> (&gt; counter n) </span><br><span class="line">            product </span><br><span class="line">            (iter (* counter product) (+ counter <span class="number">1</span>)))) </span><br><span class="line">    (iter <span class="number">1</span> <span class="number">1</span>))</span><br><span class="line">(newline)</span><br><span class="line">(display(factoral_x <span class="number">5</span>))</span><br></pre></td></tr></table></figure>

<h2 id="函数式编程的思维方式"><a href="#函数式编程的思维方式" class="headerlink" title="函数式编程的思维方式"></a>函数式编程的思维方式</h2><p>前面提到过多次，函数式编程关注的是：describe what to do, rather than how to do it。<br>于是，我们把以前的过程式编程范式叫做 Imperative Programming – 指令式编程，而把函数式编程范式叫做 Declarative Programming – 声明式编程。</p>
<h2 id="传统方式的写法"><a href="#传统方式的写法" class="headerlink" title="传统方式的写法"></a>传统方式的写法</h2><p>下面我们看一下相关的示例。比如，我们有3辆车比赛，简单起见，我们分别给这3辆车有70%的概率可以往前走一步，一共有5次机会，然后打出每一次这3辆车的前行状态。<br>对于Imperative Programming来说，代码如下(Python)：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> random <span class="keyword">import</span> random</span><br><span class="line">time = <span class="number">5</span></span><br><span class="line">car_positions = [<span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>]</span><br><span class="line"><span class="keyword">while</span> time:    </span><br><span class="line">    time -= <span class="number">1</span>                               <span class="comment"># decrease time</span></span><br><span class="line">    <span class="keyword">print</span> <span class="string">&#x27;&#x27;</span> </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(car_positions)):     </span><br><span class="line">        <span class="keyword">if</span> random() &gt; <span class="number">0.3</span>: </span><br><span class="line">            car_positions[i] += <span class="number">1</span>           <span class="comment"># move car </span></span><br><span class="line">        <span class="keyword">print</span> <span class="string">&#x27;-&#x27;</span> * car_positions[i]        <span class="comment"># draw car </span></span><br></pre></td></tr></table></figure>
<p>我们可以把这两重循环变成一些函数模块，这样有利于更容易地阅读代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> random <span class="keyword">import</span> random</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">move_cars</span>():</span> </span><br><span class="line">    <span class="keyword">for</span> i, _ <span class="keyword">in</span> enumerate(car_positions): </span><br><span class="line">        <span class="keyword">if</span> random() &gt; <span class="number">0.3</span>: </span><br><span class="line">            car_positions[i] += <span class="number">1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">draw_car</span>(<span class="params">car_position</span>):</span> </span><br><span class="line">    <span class="keyword">print</span> <span class="string">&#x27;-&#x27;</span> * car_position</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run_step_of_race</span>():</span> </span><br><span class="line">    <span class="keyword">global</span> time </span><br><span class="line">    time -= <span class="number">1</span> </span><br><span class="line">    move_cars()</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">draw</span>():</span> </span><br><span class="line">    <span class="keyword">print</span> <span class="string">&#x27;&#x27;</span> </span><br><span class="line">    <span class="keyword">for</span> car_position <span class="keyword">in</span> car_positions: </span><br><span class="line">        draw_car(car_position)</span><br><span class="line">time = <span class="number">5</span></span><br><span class="line">car_positions = [<span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>]</span><br><span class="line"><span class="keyword">while</span> time: </span><br><span class="line">    run_step_of_race() </span><br><span class="line">    draw()</span><br></pre></td></tr></table></figure>
<p>上面的代码，从主循环开始，我们可以很清楚地看到程序的主干，因为我们把程序的逻辑分成了几个函数。这样一来，代码逻辑就会变成几个小碎片，于是我们读代码时要考虑的上下文就少了很多，阅读代码也会更容易。不像第一个示例，如果没有注释和说明，你还是需要花些时间理解一下。而将代码逻辑封装成了函数后，我们就相当于给每个相对独立的程序逻辑取了个名字，于是代码成了自解释的。<br>但是，你会发现，封装成函数后，这些函数都会依赖于共享的变量来同步其状态。于是，在读代码的过程中，每当我们进入到函数里，读到访问了一个外部的变量时，我们马上要去查看这个变量的上下文，然后还要在大脑里推演这个变量的状态，才能知道程序的真正逻辑。也就是说，这些函数间必需知道其它函数是怎么修改它们之间的共享变量的，所以，这些函数是有状态的。</p>
<h2 id="函数式的写法"><a href="#函数式的写法" class="headerlink" title="函数式的写法"></a>函数式的写法</h2><p>我们知道，有状态并不是一件很好的事情，无论是对代码重用，还是对代码的并行来说，都是有副作用的。因此，要想个方法把这些状态搞掉，于是出现了函数式编程的编程范式。下面，我们来看看函数式的方式应该怎么写？</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> random <span class="keyword">import</span> random</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">move_cars</span>(<span class="params">car_positions</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> map(<span class="keyword">lambda</span> x: x + <span class="number">1</span> <span class="keyword">if</span> random() &gt; <span class="number">0.3</span> <span class="keyword">else</span> x, car_positions)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">output_car</span>(<span class="params">car_position</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;-&#x27;</span> * car_position</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run_step_of_race</span>(<span class="params">state</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&#x27;time&#x27;</span>:state[<span class="string">&#x27;time&#x27;</span>]<span class="number">-1</span>, <span class="string">&#x27;car_positions&#x27;</span>: move_cars(state[<span class="string">&#x27;car_positions&#x27;</span>])&#125;</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">draw</span>(<span class="params">state</span>):</span> </span><br><span class="line">    <span class="keyword">print</span> <span class="string">&#x27;&#x27;</span> </span><br><span class="line">    <span class="keyword">print</span> <span class="string">&#x27;\n&#x27;</span>.join(map(output_car, state[<span class="string">&#x27;car_positions&#x27;</span>]))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">race</span>(<span class="params">state</span>):</span> </span><br><span class="line">    draw(state) </span><br><span class="line">    <span class="keyword">if</span> state[<span class="string">&#x27;time&#x27;</span>]: </span><br><span class="line">        race(run_step_of_race(state))</span><br><span class="line">race(&#123;<span class="string">&#x27;time&#x27;</span>: <span class="number">5</span>, <span class="string">&#x27;car_positions&#x27;</span>: [<span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>]&#125;)</span><br></pre></td></tr></table></figure>
<p>上面的代码依然把程序的逻辑分成了函数。不过这些函数都是函数式的，它们有三个特点：它们之间没有共享的变量；函数间通过参数和返回值来传递数据；在函数里没有临时变量。</p>
<p>我们还可以看到，for循环被递归取代了(见race函数)—— 递归是函数式编程中常用到的技术，正如前面所说的，递归的本质就是描述问题是什么。</p>
<h2 id="函数式语言的三套件"><a href="#函数式语言的三套件" class="headerlink" title="函数式语言的三套件"></a>函数式语言的三套件</h2><p>函数式语言有三套件，Map、Reduce和Filter。这在谈C++的泛型编程时已经介绍过。下面我们来看一下Python语言中的一个示例。这个示例的需求是，我们想把一个字符串数组中的字符串都转成小写。<br>用常规的面向过程的方式，代码如下所示：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 传统的非函数式</span></span><br><span class="line">upname =[<span class="string">&#x27;HAO&#x27;</span>, <span class="string">&#x27;CHEN&#x27;</span>, <span class="string">&#x27;COOLSHELL&#x27;</span>]</span><br><span class="line">lowname =[] </span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(upname)): </span><br><span class="line">    lowname.append(upname[i].lower())</span><br></pre></td></tr></table></figure>
<p>如果写成函数式，用map()函数，是下面这个样子。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 函数式</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">toUpper</span>(<span class="params">item</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> item.upper()</span><br><span class="line">upper_name = map(toUpper, [<span class="string">&quot;hao&quot;</span>, <span class="string">&quot;chen&quot;</span>, <span class="string">&quot;coolshell&quot;</span>])</span><br><span class="line"><span class="keyword">print</span> upper_name                   </span><br><span class="line"><span class="comment"># 输出 [&#x27;HAO&#x27;, &#x27;CHEN&#x27;, &#x27;COOLSHELL&#x27;]</span></span><br></pre></td></tr></table></figure>
<p>顺便说一下，上面的例子是不是和我们 C++ 语言中的 STL 的 transform() 函数有些像？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">string</span> s=<span class="string">&quot;hello&quot;</span>;</span><br><span class="line">transform(s.begin(), s.end(), back_inserter(out), ::<span class="built_in">toupper</span>);</span><br></pre></td></tr></table></figure>
<p>在上面Python的那个例子中可以看到，我们定义了一个函数toUpper，这个函数没有改变传进来的值，只是把传进来的值做个简单的操作，然后返回。然后，我们把其用在map函数中，就可以很清楚地描述出我们想要干什么。而不会去理解一个在循环中怎么实现的代码，最终在读了很多循环的逻辑后才发现原来是这个或那个意思。<br>如果你觉得上面的代码在传统的非函数式的方式下还是很容易读的，那么我们再来看一个计算数组平均值的代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 计算数组中正数的平均值</span></span><br><span class="line">num = [<span class="number">2</span>, <span class="number">-5</span>, <span class="number">9</span>, <span class="number">7</span>, <span class="number">-2</span>, <span class="number">5</span>, <span class="number">3</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">-3</span>, <span class="number">8</span>]</span><br><span class="line">positive_num_cnt = <span class="number">0</span></span><br><span class="line">positive_num_sum = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(num)): </span><br><span class="line">    <span class="keyword">if</span> num[i] &gt; <span class="number">0</span>: </span><br><span class="line">        positive_num_cnt += <span class="number">1</span> </span><br><span class="line">        positive_num_sum += num[i]</span><br><span class="line"><span class="keyword">if</span> positive_num_cnt &gt; <span class="number">0</span>: </span><br><span class="line">    average = positive_num_sum / positive_num_cnt</span><br><span class="line">    <span class="keyword">print</span> average</span><br></pre></td></tr></table></figure>
<p>上面的代码如果没有注释的话，你需要看一会儿才能明白，只是计算数组中正数的平均值。<br>我们再来看看函数式下使用 filter/reduce 函数的玩法。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 计算数组中正数的平均值</span></span><br><span class="line">positive_num = filter(<span class="keyword">lambda</span> x: x&gt;<span class="number">0</span>, num)</span><br><span class="line">average = reduce(<span class="keyword">lambda</span> x,y: x+y, positive_num)/len(positive_num)</span><br></pre></td></tr></table></figure>
<p>首先，我们使用 filter 函数把正数过滤出来(注意：lambda x: x&gt;0 这个lambda表达式)，保存在一个新的数组(positive_num)中。然后，我们使用reduce函数对数组positive_num求和后，再除以其个数，就得到正数的平均值了。<br>我们可以看到，隐藏了数组遍历并过滤数组控制流程的filter和reduce，不仅让代码更为简洁，因为代码里只有业务逻辑了，而且让我们能更容易地理解代码。<br>对num数组filter条件x&gt;0的数据。然后对positive_num进行x+y操作的reduce，即求和。<br>感觉代码更亲切了，不是吗？因为：</p>
<ul>
<li>数据集、对数据的操作和返回值都放在了一起。</li>
<li>没有了循环体，就可以少了些临时用来控制程序执行逻辑的变量，也少了把数据倒来倒去的控制逻辑。</li>
<li>代码变成了在描述你要干什么，而不是怎么干。</li>
</ul>
<p>当然，如果你是第一次见到 map/reduce/filter，那你可能还是会有点儿陌生和不解，这只是你不了解罢了。<br>对于函数式编程的思路，下图是一个比较形象的例子，面包和蔬菜map到切碎的操作上，再把结果给reduce成汉堡。<br><img src="http://5b0988e595225.cdn.sohucs.com/images/20180212/f99feac016bf4663809955858181eb2c.jpeg" alt="img"><br>在这个图中，我们可以看到 map 和 reduce 不关心源输入数据，它们只是控制，并不是业务。控制是描述怎么干，而业务是描述要干什么。</p>
<h2 id="函数式的pipeline模式"><a href="#函数式的pipeline模式" class="headerlink" title="函数式的pipeline模式"></a>函数式的pipeline模式</h2><p>pipeline(管道)借鉴于Unix Shell的管道操作——把若干个命令串起来，前面命令的输出成为后面命令的输入，如此完成一个流式计算。(注：管道绝对是一个伟大的发明，它的设计哲学就是KISS – 让每个功能就做一件事，并把这件事做到极致，软件或程序的拼装会变得更为简单和直观。这个设计理念影响非常深远，包括今天的Web Service、云计算，以及大数据的流式计算等。)<br>比如，我们如下的 shell 命令：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ps auwwx | awk <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> | sort -n | xargs echo</span><br></pre></td></tr></table></figure>
<p>上面的例子是要查看一个用户执行的进程列表，列出来以后，然后取第二列，第二列是它的进程ID，排个序，再把它显示出来。抽象成函数式的样子，我们就可以反过来，一层套一层。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">xargs( echo, sort(n, awk(<span class="string">&#x27;print $2&#x27;</span>, ps(auwwx))) )</span><br></pre></td></tr></table></figure>
<p>我们也可以把函数放进数组里面，然后顺序执行一下。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pids = for_each(result, [ps_auwwx, awk_p2, sort_n, xargs_echo])</span><br></pre></td></tr></table></figure>
<p>多说一句，如果我们把这些函数比作微服务，那么管道这个事是在干什么呢？其实就是在做服务的编排。像Unix这些经典的技术上的实践或理论，往往是可以反映到分布式架构的，所以，一般来说，一个好的分布式架构师，通常都是对这些传统的微观上的经典技术有非常深刻的认识，因为这些东西在方法论上都是相通的。好了，还是让我们用一个简单的示例来看一下如何实现pipeline。<br>我们先来看一个程序，这个程序的process()有三个步骤：</p>
<ul>
<li>找出偶数；</li>
<li>乘以 3；</li>
<li>转成字符串返回。</li>
</ul>
<p>传统的非函数式的实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process</span>(<span class="params">num</span>):</span> </span><br><span class="line">    <span class="keyword">if</span> num % <span class="number">2</span> != <span class="number">0</span>:                        <span class="comment"># filter out non-evens </span></span><br><span class="line">        <span class="keyword">return</span> </span><br><span class="line">    num = num * <span class="number">3</span> </span><br><span class="line">    num = <span class="string">&#x27;The Number: %s&#x27;</span> % num </span><br><span class="line">    <span class="keyword">return</span> num</span><br><span class="line">nums = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>, <span class="number">10</span>]</span><br><span class="line"><span class="keyword">for</span> num <span class="keyword">in</span> nums: </span><br><span class="line">    <span class="keyword">print</span> process(num)</span><br><span class="line"><span class="comment"># 输出：</span></span><br><span class="line"><span class="comment"># None</span></span><br><span class="line"><span class="comment"># The Number: 6</span></span><br><span class="line"><span class="comment"># None</span></span><br><span class="line"><span class="comment"># The Number: 12</span></span><br><span class="line"><span class="comment"># None</span></span><br><span class="line"><span class="comment"># The Number: 18</span></span><br><span class="line"><span class="comment"># None</span></span><br><span class="line"><span class="comment"># The Number: 24</span></span><br><span class="line"><span class="comment"># None</span></span><br><span class="line"><span class="comment"># The Number: 30</span></span><br></pre></td></tr></table></figure>
<p>我们可以看到，输出的结果并不够完美，另外，代码阅读上如果没有注释，你也会比较晕。下面，我们来看看函数式的 pipeline(第一种方式)应该怎么写？<br>第一步，我们先把三个“子需求”写成函数：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">even_filter</span>(<span class="params">nums</span>):</span> </span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums: </span><br><span class="line">        <span class="keyword">if</span> num % <span class="number">2</span> == <span class="number">0</span>: </span><br><span class="line">            <span class="keyword">yield</span> num</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">multiply_by_three</span>(<span class="params">nums</span>):</span> </span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums: </span><br><span class="line">        <span class="keyword">yield</span> num * <span class="number">3</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_to_string</span>(<span class="params">nums</span>):</span> </span><br><span class="line">    <span class="keyword">for</span> num <span class="keyword">in</span> nums: </span><br><span class="line">        <span class="keyword">yield</span>  <span class="string">&#x27;The Number: %s&#x27;</span> % num</span><br></pre></td></tr></table></figure>
<p>然后，我们再把这三个函数串起来：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nums = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>, <span class="number">10</span>]</span><br><span class="line">pipeline = convert_to_string(multiply_by_three(even_filter(nums)))</span><br><span class="line"><span class="keyword">for</span> num <span class="keyword">in</span> pipeline: </span><br><span class="line">    <span class="keyword">print</span> num</span><br><span class="line"><span class="comment"># 输出：</span></span><br><span class="line"><span class="comment"># The Number: 6</span></span><br><span class="line"><span class="comment"># The Number: 12</span></span><br><span class="line"><span class="comment"># The Number: 18</span></span><br><span class="line"><span class="comment"># The Number: 24</span></span><br><span class="line"><span class="comment"># The Number: 30</span></span><br></pre></td></tr></table></figure>
<p>上面，我们动用了Python的关键字yield，它是一个类似return的关键字，只是这个函数返回的是Generator(生成器)。所谓生成器，指的是yield返回的是一个可迭代的对象，并没有真正的执行函数。也就是说，只有其返回的迭代对象被迭代时，yield函数才会真正运行，运行到yield语句时就会停住，然后等下一次的迭代。(yield 是个比较诡异的关键字)这就是lazy evluation(懒惰加载)。<br>好了，根据前面的原则——“使用Map&amp;Reduce，不要使用循环”(还记得吗？使用循环会让我们只能使用顺序型的数据结构)，那我们用比较纯朴的Map&amp;Reduce吧。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">even_filter</span>(<span class="params">nums</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> filter(<span class="keyword">lambda</span> x: x%<span class="number">2</span>==<span class="number">0</span>, nums)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">multiply_by_three</span>(<span class="params">nums</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> map(<span class="keyword">lambda</span> x: x*<span class="number">3</span>, nums)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_to_string</span>(<span class="params">nums</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> map(<span class="keyword">lambda</span> x: <span class="string">&#x27;The Number: %s&#x27;</span> % x, nums)    </span><br><span class="line">nums = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>, <span class="number">10</span>]</span><br><span class="line">pipeline = convert_to_string( multiply_by_three( even_filter(nums) ) )</span><br><span class="line"><span class="keyword">for</span> num <span class="keyword">in</span> pipeline: </span><br><span class="line">    <span class="keyword">print</span> num</span><br></pre></td></tr></table></figure>
<p>上面的代码是不是更容易读了，但需要嵌套使用函数，这个有点儿不爽，如果我们能像下面这个样子就好了(第二种方式)。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pipeline_func(nums, [even_filter, multiply_by_three, convert_to_string])</span><br></pre></td></tr></table></figure>
<p>可以看到，其实，就是对一堆函数做一个reduce，于是，pipeline函数可以实现成下面这样：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pipeline_func</span>(<span class="params">data, fns</span>):</span> <span class="keyword">return</span> reduce(<span class="keyword">lambda</span> a, x: x(a), fns, data)</span><br></pre></td></tr></table></figure>
<p>当然，使用Python的force函数以及decorator模式可以把上面的代码写得更像管道：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pipe</span>(<span class="params">object</span>):</span> </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, func</span>):</span> </span><br><span class="line">        self.func = func</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__ror__</span>(<span class="params">self, other</span>):</span> </span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">generator</span>():</span> </span><br><span class="line">            <span class="keyword">for</span> obj <span class="keyword">in</span> other: </span><br><span class="line">                <span class="keyword">if</span> obj <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>: </span><br><span class="line">                    <span class="keyword">yield</span> self.func(obj) </span><br><span class="line">        <span class="keyword">return</span> generator()</span><br><span class="line"><span class="meta">@Pipe</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">even_filter</span>(<span class="params">num</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> num <span class="keyword">if</span> num % <span class="number">2</span> == <span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line"><span class="meta">@Pipe</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">multiply_by_three</span>(<span class="params">num</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> num*<span class="number">3</span></span><br><span class="line"><span class="meta">@Pipe</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_to_string</span>(<span class="params">num</span>):</span> </span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;The Number: %s&#x27;</span> % num</span><br><span class="line"><span class="meta">@Pipe</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">echo</span>(<span class="params">item</span>):</span> </span><br><span class="line">    <span class="keyword">print</span> item </span><br><span class="line">    <span class="keyword">return</span> item</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">force</span>(<span class="params">sqs</span>):</span> </span><br><span class="line">    <span class="keyword">for</span> item <span class="keyword">in</span> sqs: </span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">nums = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>, <span class="number">10</span>]</span><br><span class="line">force(nums | even_filter | multiply_by_three | convert_to_string | echo)</span><br></pre></td></tr></table></figure>

<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>相对于计算机发展史，函数式编程是个非常古老的概念，它的核心思想是将运算过程尽量写成一系列嵌套的函数调用，关注的是做什么而不是怎么做，因而被称为声明式编程。以Stateless(无状态)和Immutable(不可变)为主要特点，代码简洁，易于理解，能便于进行并行执行，易于做代码重构，函数执行没有顺序上的问题，支持惰性求值，具有函数的确定性——无论在什么场景下都会得到同样的结果。</p>
<p>本文结合递归、map和reduce，以及pipeline等技术，对比了非函数式编程和函数式编程在解决相同问题时的不同处理思路，让你对函数式编程范式有了清晰明确的认知。并在文末引入了decorator(修饰器)，使得将普通函数管道化成为一件轻而易举的事情。此时你可能有疑问，decorator到底是什么呢？怎样使用它呢？敬请关注下一章中的内容，来得到这些答案。</p>
<p>作者｜陈皓<br>编辑｜杨爽</p>
]]></content>
      <tags>
        <tag>Python</tag>
        <tag>程序员的自我修养</tag>
        <tag>函数式编程</tag>
        <tag>转载</tag>
      </tags>
  </entry>
</search>
